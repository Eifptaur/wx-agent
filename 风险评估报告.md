# wx-agent 项目整体风险评估报告

> 版本：vβ·Ⅱ　评估日期：2026-09-08
> 结论（综合用户实测与接管调研）：**整体风险度：低**。

---

## 一、风险度总评

- **用户实机验证**：本项目已在真实环境中连续运行约 3~4 天，未出现影响使用的功能性问题（收发消息、自检、控制台均正常）。
- **接管调研后的修复**：接管过程发现并修复了一批"潜伏但会出错"的问题（见第四节），修复前后均做了**真机鼠标验证**。这些验证本身降低了"带病上线"的风险。
- **判定**：整体**低风险**。风险主要不是"会不会坏"，而是"**微信官方更新界面 / 用户换电脑**"这类**外部变化**带来的适配问题，属于可预期、可恢复的运维风险，而非架构性缺陷。

---

## 二、各模块风险分析

| 模块 | 风险点 | 等级 | 当前状态 |
|---|---|---|---|
| 微信接入（UIA 无注入读库+键鼠） | 微信版本/界面变化导致坐标或 UIA 失效 | **中** | 已做版本体检+看门狗；版本不符自动弹窗修正 |
| 表情面板（原生收藏表情） | 平滑滚动并吞事件、格为固定物理尺寸、顶部/底部半行、贴纸跨栏破坏序号 | **中（已缓解）** | 已修：滚轮方向/点格中心/自适应/半行+最后一行；21/27/24/31 真机全对 |
| 朋友圈（moments_*） | 侧栏图标坐标若未随窗口缩放会点偏；赞/评发送会打扰真人 | **低（已缓解）** | 已修图标坐标缩放；检验改为"到菜单/输入框"不执行最后一步 |
| 控制台 UI（鲸语/导航/光标/日历） | 保存被旧值覆盖、导航滚到底分层、光标重置残留、预览缓存 | **低（已缓解）** | 已修：鲸语保存、侧栏去 fit-content 双高、光标重置删残留、新增日历 |
| 程序鼠标检验/一键体检 | 检验若不注意会在真人会话操作 | **低（已缓解）** | 检验默认走「文件传输助手」；朋友圈赞/评 dry 不真发 |
| 发布（打包/上传/GitHub） | 包内含 config.json（密钥）泄露 | **低** | 打包脚本已排除 config.json / data / logs / media / offline / _scratch |
| 兼容性（DPI/多屏/分辨率） | 坐标若用固定像素会在其他 DPI 漂移 | **中（已缓解）** | 已改为**渲染区比例 + 检测格中心**，随窗口/DPI 缩放；_force_geometry 固定窗口标准尺寸 |
| 安全（API Key/账号） | config.json 含真实 Key | **低** | config.json 已 gitignore，且打包排除 |

---

## 三、主要剩余风险点（可接受，需持续关注）

1. **微信官方更新**：微信 4.x 界面若改版，坐标/OCR/UIA 锚点可能失效。已有"版本体检 + 卡死守护 + 重新标定"兜底，属**可自愈**。
2. **换电脑**：若用户换机，需重新标定侧栏图标/窗口几何。已有 `ui.moments_entry` 可填入口坐标、`calibrate_ui` 自动标定、`coord_scale` 兜底。
3. **收藏表情库数量/顺序**：表情面板顶部有"跨栏贴纸"会破坏"5列×N行"的序号换算（当前 index=30 已稳定命中；若增至更大索引且遇跨栏贴纸，需"读网格"增强）。属**已知且已定位**，非突发。
4. **控制台 UI 视觉细节**（导航栏质感、光标"上传后渲染"）大多依赖浏览器/微信真机画面，接管端无法自行截图，建议由用户按验收图确认后定稿。

---

## 四、接管调研发现并修复的问题（供追溯）

> 以下为本次接管逐项实测发现、修复并验证的问题记录，已写入 `开发交接.md` / `更新日志.md`：

| 编号 | 问题 | 根因 | 修复 | 验证 |
|---|---|---|---|---|
| ① | 第 31 个表情发成别的（差一行/一列） | **滚轮方向写反**（-wheel 到顶、+wheel 向下）+ 点击没在格中心 + 格是**固定物理尺寸**（105×107px、行距 132px）不随窗口缩放 + 顶部/底部 **85px 半行** + 贴纸跨栏 | 改为：+wheel=顶、-wheel=底；**滚动后检测表情格中心**点击；`-150/行`（-300 会滚 2 行）；中间行用视口 base + 行距×click_row、最后一行用"检测最底部完整行中心" | 21/27/24/31 真机全对（猫/列宁/何いもの/这凝聚程稿） |
| ② | 朋友圈点不到 | 侧栏图标 `sidebar_items` 用**标定时窗口的绝对 y**（旧 render_h=1575），当前窗口（1146）不缩放 | 按**当前窗口高缩放**（`items[order]*sh/标定sh`） | moments_open 实测打开朋友圈窗口 |
| ③ | 表情面板发送校验失败 | 旧实例/面板坐标问题 + 校验在真机会话操作 | 面板修复 + 检验转**文件传输助手** | emoji_panel 校验通过 |
| ④ | 鲸语切换保存仍"正常" | `saveAllBtn` 保存时被「原始JSON」旧值覆盖界面改动（text_style 丢失） | 保存以**界面表单（syncFromForm）为主** | 截图确认鲸语文本已显示 |
| ⑤ | 导航栏滚到下面分层 | `.side` `height:fit-content` 双高，背景与可视高不一致 | 去掉 fit-content、滚动条做半透明融合 | 初始截图单层正常 |
| ⑥ | 重置为默认鲸鱼后预览仍显示旧文件 | 只清配置，`custom-cursor.png` 文件残留，刷新后又探测到 | 新增 `/api/cursor/reset` 删残留文件+前端先删再清配置 | 待浏览器确认 |
| ⑦ | 检验打扰真人 | 检验默认在真人群/真操作赞评 | 检验默认**文件传输助手**；moments_like 到"可点赞菜单"未真赞、comment dry 到输入框未真发 | 代码+说明已更新 |
| ⑧ | （新增）概览看当天明细 | 无按日查看入口 | 概览旁新增**日历** + `/api/stats/cal` 按 `data/sessions/YYYY-MM-DD.jsonl` 汇总 | 后端自测正确、前端渲染正常 |

---

## 五、建议

1. **微信官方更新后先跑「检查微信版本.bat / 一键体检」**：失败自动重标定，风险可控。
2. **控制台 UI 视觉项**（导航栏质感、光标"上传后渲染"、日历点选）建议按验收图**人工截图确认后定稿**（接管端无浏览器画面，只能改到逻辑正确）。
3. **发布前**：源码/离线包均**不含 config.json**；上传 GitHub 前请确认版本号（`vβ·Ⅱ`）与授权（铁律：用户确认后才上传）。
4. **长期**：若收藏表情库继续增多且遇跨栏贴纸，可评估"读网格"增强（已知升级路径，非当前必需）。

---

**总体判断**：项目当前**风险低**，可放心发布/长期运行；主要关注点是"外部环境变化"（微信更新、换机），均有兜底机制，属于可接受、可恢复的运维风险。
