# wx-agent 交接说明（新会话必读）

> 版本 v2.0.0·Ⅱ，仓库 `C:\Users\ptmou\Desktop\WX-chatbot\wx-agent`。
> 本文件供下一会话接手：已完成 / 未完成 / 关键文件 / 关键陷阱 / 下一步。

## 一、表情第31个：已修正、已自适应、实测通过 ✅
- **现象（历史）**：点第31个表情包（=第7行第1个=**最后一行第一个，是只猫**），过去发成别的（如第4行第1个仓鼠）。
- **根因（确认并修复）**：`agent/wechat.py::emoji_panel_send` 的**滚轮方向写反**——原用 `-wheel` 到顶、`+wheel` 向下。
  实测：`+wheel`=向**顶部/更早**滚；`-wheel`=向**后面/底部**滚。
- **自适应几何（用户实测数据，全部用「渲染窗比例」随 DPI/分辨率缩放；窗口被 `_force_geometry` 固定标准尺寸）**：
  · 5 列；第一格中心 `(0.092, 0.277)`；列距 `0.107`；行距 `0.125`（=表情图107px + 间隙23px ≈ 132px/格 @150% DPI）。
  · 顶/底特殊半行 ≈ `0.081`（=85px，用户实测 84/86）；目标行只在完整行内点。
  · 滚轮方向 +wheel=顶、-wheel=底；**滚一行所需 wheel 单位 = `(0.125×sh)/0.44`**（0.44=每 wheel 单位约滚 0.44 渲染px，实测），
    随 DPI/分辨率自适应。每格滚动留 ≥0.35~0.5s（微信平滑滚动并吞快速事件）。
- **点击正中心（用户指出的根因，已修复）**：表情格是**固定物理尺寸**（表情图≈105×107px、行距≈132px），**不随窗口 sh 缩放**，
  原用「窗口比例」算点击中心会在 sh 变化后漂移偏上、点到上一格。已改为：滚动后 `_emoji_row_center(gui)` **实时截图检测
  「第一列最底部完整行」中心**作为点击 y（x 仍用列比例），保证点击落在格正中心、自适应任意 DPI/窗口。失败回退比例。
- **点击正中心（用户指出的根因，已修复）**：表情格是**固定物理尺寸**（表情图≈105×107px、行距≈132px、顶/底半行≈85px），**不随窗口 sh 缩放**，
  原用「窗口比例」算点击中心会在 sh 变化后漂移偏上、点到上一格。
- **最终成功方案（`emoji_panel_send`，实测 21/27/24/31 全对）**：
  · 滚轮方向：+wheel=到顶、-wheel=到底；**每行用 `-150`（-300 会一次滚约2行导致滚过头）**；到顶用 +wheel×12，每格留 ≥0.35~0.5s。
  · 滚 `_rolls = row - 3` 行，`click_row = 3`（最后一行滚到底，`click_row` 仍 3）。
  · **点击 y**：中间行（row<6）= `视口base + click_row×132`（base=`_emoji_base_center` 检测到顶后最顶部完整行中心，视口固定位置）；
    **最后一行（row≥6，滚到底）= `_emoji_bottom_center` 检测最底部完整行中心**。x = `列比例`。
  · 关键：**到底时顶部那行是半行、到顶时底部那行是半行**（用户澄清），所以最后一行不能用 base+行距（会偏），要用检测最底部完整行中心。
  · 实测：index 20/26/23/30 → 第21/27/24/31（列宁/这凝聚程稿/何いもの/猫）全部正确，纯程序自主判断（非看图）。
- **加速探索结论**：盲压「搜完群→点笑脸」等待会因面板未就绪点偏格（实测发成水滴）——**别压这些必要等待**；
  提速靠「点击格中心准确化」（已做）+ 减少非必要步骤，而非压 UI 稳定等待。
- **实测通过（重复稳定）**：`/api/ui-test kind=emoji_panel&index=30&group=aa` → **aa 群 09:14、09:24 两次都发出那只猫**
  「难道我是一只很坏很坏的小猫咪吗？」（=第7行第1个）。点击前十字截图 `_scratch/shots/panel_click_30.png`。09:08 仓鼠是修复前旧结果。
- **遗留（增强项）**：顶部「有权给任何人」仓鼠**跨栏贴纸**会破坏「5列×N行」的 index→行 换算；任意 index 精准需「读网格」。index=30 已稳定命中。
- **用户同意流程**：先滚到顶 → 判断行 → 决定是否滚 → 从顶往下滚；**只准在 aa 群测**，绝不动海绵宝宝群。
- 调试口：`/api/ui-test kind=emoji_panel&index=N&group=aa`（点击前存十字截图 `panel_click_N.png`）+ `kind=emoji_roll_probe&delta=D&group=aa`。

## 二、表情面板链路（已全链跑通，实测通过；坐标已按本机复测微调）
- 流程：搜索群聊进群(`emoji_panel_open(group)`) → 点笑脸(只点一次) → 面板弹 → 点爱心(标签行最右) → 点第 N 个表情格(单击即发)。
- **关键坐标（面板内/窗口比例，复测）**：
  - 笑脸 = `(0.302*sw, 0.879*sh - 18)`（`_emoji_btn_pos`）
  - 爱心 = `(0.171*sw, 0.804*sh - 13)`
  - 网格第一格中心 = `(0.092*sw, 0.277*sh)`，列距 `0.107*sw`，行距 `0.125*sh`（=132px），**一行 5 列**；
    可见完整行 = 4；第 N 个：col=N%5, row=N//5（**注意：有跨栏贴纸时序号换算会错位**）。
- **只点一下**；**send 里绝不调 prepare_screen**；表情点击全部 `heal=False`；不用 Esc；流程中绝不 SetWindowPos。
- 测试：`emoji_panel_open("aa")` → `emoji_panel_send(index)`。
- 说明：原交接声称「第3/16/22/31 均正确发出」**不可尽信**——本机复测发现平滑滚动+贴纸导致 index→行 换算不准。

## 三、已交付（可靠，勿改坏）
- **几何恒定**：`agent/ui_adapt.py::_force_geometry`（SetWindowPos 固定 120,80,1250×1100×DPI）。注意：**只应在初始化/出屏自愈时调用**，流程中若每次 SetWindowPos 会把表情弹出菜单刷掉（卡死/菜单消失根因）。
- **主窗置前**：`ensure_point` 用 `SetForegroundWindow(main_hwnd)`，**禁用 wechatauto `bring_to_front`**（会顶起 WeixinClawBot 子窗）。
- **主线程微信接入**（`wx_agent.py` 微信段）：微信对象必须在主线程构造/使用（UIA/COM 跨线程死锁）。
- **单浏览器窗口**（枚举已有同 URL 进程）；**端口 3210**（config.json server.port）。
- **概览两格**：本次其他工具成本 `extra_now_cost` / 累计 `extra_total_cost`（`data/tool_usage.json`，`agent/llm.py set_usage_hook` 记账）。
- **人设选单**：标题「人设选单」；排序按钮（按评估分）；星标置顶（persona_favs.json）；卡片右下 ⋯ 菜单（含"为模型打星"）；**已删系统贴合分显示**（只显示模型分 xx.xx）。
- **评分细则 v2.0**（0.01 精度）：`agent/persona_rating.py`（五维加权 风格25/贴合30/一致20/自然15/完整10 + 扣分表 + 满分唯一条件）。模型评分/补足（`persona_score_custom_fn`/`persona_ai_enrich_fn`）**联网真调用，非本地**；补足=人设导向+轮数(1~3)+分升继续。
- **素材库 1052 条**（`agent/scoring.py` seed_library(limit=12)=取样12、防饱和去近重+池内相似>0.8 重抽；MAX_POOL=2000）。
- **代码检测**：`agent/code_check.py` 51+ 项历史回归（检验清单），实测 0.33 秒。
- **导航实底色** `rgba(12,32,58,1)`（滚动色差修复）。
- **光标预览**：`preImg.onload=drawPreview`（换源重绘）。
- **鲸语服务端切换**：`_apply_whale`+`Cache-Control:no-store`+保存后 `location.replace(?v=ts)`（双向切换）。
- 一句话启动 / onestart 单实例 / 后台微信接入（控制台先行）。

## 四、仍未验收/完成（需逐项截图通过才算）
1. **表情滚动量校准**（最重要，见第一节）。
2. **朋友圈**：`agent/wechat.py` moments_* 系列。已知：微信4.x朋友圈窗口标题=「Weixin」（非「朋友圈」，已放宽匹配）；`wechat_ui` `_layout_stale` 自动重标定（窗口尺寸>15% 变化）。但仍不稳定，需真机逐步截图定位。
3. **代码检测在旧实例卡**：新包已 0.33s；**用户需重启实例**用新包验证（旧实例无 `/api/code-check` 接口，点击永远等待）。
4. **导航实色 / 光标预览 / 鲸语切换**：已改代码，**未在用户环境逐张截图验收**。
5. **暂停按钮同步（032534）**：状态=已暂停时按钮应显示「恢复」；点击按当前态执行。待截图。
6. **模型-程序协作表情系统**（已搭好数据层，待联动验证）：
   - 已建 `agent/emoji_lib.py`（概述生成 `gen_summary` / 索引入库 `record` / 清单 `list_summaries` /
     模型选择 `pick`，持久化到 `data/emoji_index.json`）；自检 `py -3 -m agent.emoji_lib` 通过。
   - 已接线：`agent/tools.py` `collect_emoji` 成功收藏后 `emoji_lib.record(概述, -1)`；
     `send_emoji` 改用 `emoji_lib`（按 name/概述匹配→否则模型 `pick` 说话语境→`emoji_panel_send(index)`）。
   - **待联动**：index→面板格的映射（需要①的滚动校准 + 读取网格），以及收藏后重扫描网格刷新 index。
   - 模型收藏时生成极简概述入库（总数/行数/顶部底部行/每表情列序/概述）；发表情时把全部概述报给模型→返回选第几个→换算行列→滚动→点选发送。

## 五、关键陷阱（务必别踩）
- **动微信窗口 = 表情菜单被刷掉**（prepare_screen 每 click 别 SetWindowPos/bring_to_front）。
- **heal 抖动会取消弹出菜单**（表情点击 heal=False）。
- **Esc 易卡死微信**（关面板用点空白）。
- **get_input_box 长循环（6次+重校准）** 是高 UIA 卡死源，别频繁调。
- **坐标要按 5 列**（用户截图：一行5个，4 完整行，半行在顶/底）。
- **微信必须登录态**（否则 open_chat 全失败回退文件传输助手）；测试前确认无「需在手机上完成登录」弹窗。
- 测试脚本常重建 WeChatAdapter(慢/校准) → 真实长驻 bot 快；测性能用长驻实例的 `/api/ui-test?kind=emoji_panel`。

## 六、待办用户确认项
- 上传需用户确认；说明/更新日志/文档同步改；提交信息=「文件名 功能说明」。
