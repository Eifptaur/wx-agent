# ── 敏感文件（含 API Key、访问口令，绝不能上传）──
config.json
config/*.txt

# ── 运行时生成（聊天存档/记忆/图片/日志/统计，均含隐私）──
data/
logs/
media/
backups/
*.log

# ── Python 缓存 ──
__pycache__/
*.pyc
*.pyo

# ── 离线部署包：wheels + 绿色版 Python 随仓库发布；微信安装包不在内（腾讯版权，请自行安装微信 4.x）──
offline/*
!offline/wheels/
!offline/python/
!offline/wheels/**
!offline/python/**

# ── 系统 / 编辑器 ──
.DS_Store
Thumbs.db
desktop.ini
.idea/
.vscode/

# ── 其他压缩包（离线 Python zip 除外）──
*.zip
!offline/python/*.zip

# ── 本地私有文档（交接/笔记，不随仓库分发）──
接手说明.md
