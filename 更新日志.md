# 更新日志（Changelog）

按时间倒序。每条写明「日期：功能 / 修复」。

## 2026-09-06（v2.0.0 正式版）

### 功能
- 微信接入：UIA 无注入（读本地库 + 模拟键鼠），多群轮询、目标群白名单、响应档位 1~4、发送限频与真人化间隔、新对话开始 70% 自动引用对方最近一句
- 多模型：12+ 家厂商 80+ 型号（DeepSeek/V3.2、Kimi/K3/K2.6、智谱 GLM-5.2/5.3、通义 Qwen3.7/3.8、MiniMax M3/M2.7、豆包、ChatGPT/GPT-5.6 系、Claude Opus5/Sonnet5/Fable5、Gemini 3.6/3.7、Grok 4.5/4.6、NVIDIA Nemotron3、OpenRouter 聚合）；控制台一键切换、按厂商存 Key（provider_keys）、切换回填、自定义 BaseURL
- 拍一拍：右键头像/气泡双路径、自动回拍（90% + 30 分钟冷却）、主动皮一下低频（10% + 每日 3 次）、数据库验证
- 识图（多模态）/ 发图 / 联网搜索（多引擎 + SSRF 防护）/ 群友长期记忆（自动整理）
- Web 控制台：一键体检（51 项）、拍一拍诊断、运行明细（每轮思考/token/工具）、今日/本周/累计用量与成本、小鲸鱼余额挂件、三步首次向导（Key→选群→体检）
- 无窗口运行：VBS + pythonw 看门狗（崩溃自动重启）、停止器按 PID 静默结束、开机自启（wscript）、零 PowerShell
- 版本体检：启动自动检测微信/适配层/14 项依赖，不匹配弹窗询问自动修正；微信卡死守护（30 秒心跳，2 次无响应自动重启微信并提醒登录）
- 离线部署包：依赖 wheels + 绿色版 Python 3.10 + 微信 4.1.13 安装包
- 价目表：12 厂商 50 行全模型单价（官方价/估算标注、每轮成本与 10 元轮数自动重算）

### 修复
- 引用失效：右键改为气泡文本定位（头像中心 +70~130px、OCR 文本行中心 y），翻页查找滚轮方向修正，UIA 直输输入框（引用模式像素探测失效），找不到不乱点
- 拍一拍误报/失败：头像折叠组改气泡菜单、OCR 菜单过滤（@/长文本/行高剔除）、翻页后行居中
- 点击归属：被窗口遮挡时自动把微信置前重试；Esc 仅在确认真实菜单时按（防误关微信）
- 程序发送与模型回答互抢窗口（全局串行锁）
- noreply 沉默：模型未调发送工具但写了最终文本时自动补发
- 成本恒 0：未知型号按默认档估算；用量持久化 + 按周期重置（今日/本周/累计）
- bat/VBS 中文乱码：GBK+CRLF 重写全部批处理；自检.bat 移到根目录
- 控制台：Logo 弹跳物理动画、导航滚动同步、厂商下拉缺项（Grok/NVIDIA/OpenRouter）、detectProvider 被误删恢复
- 微信版本检测、基础库版本校验

### 已知限制
- 收藏表情库/合并转发未实现（微信自绘 UI）
- 海外端点需可访问该厂商的网络（地区限制除外）
- 数据库/微信界面随官方更新可能需升级适配层（体检会提示）
