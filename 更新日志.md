更新日志（Changelog）

说明：本文件按时间倒序记录 wx-agent 的每次改动。每条写清楚「日期、改了什么、有什么用处」。最新的在最上面。

2026-09-06　说明修正：上手零配置（三步向导，不再"复制 config 改文件"）
改动：
1. README/使用说明「快速安装」改为零文件操作：装依赖 → 启动 vbs（自动开控制台+首次三步向导：填 Key/勾选群/体检）→ 群里 @ 测试；所有设置都在控制台改、保存即生效（config.json 自动生成维护，仅作底层速查）。
2. 清除全部"复制 config.example.json 改名填写"的过时表述；Release v2.0.0 描述同步（上手零配置+三步向导）。
好处：新电脑/给别人用 10 分钟上线，不再碰任何文件。

2026-09-06　模型全面扩充：10 家厂商 60+ 型号（含 Kimi K3、GPT-5、Claude 4-1/4-5、Gemini 3、Grok）
改动：
1. 控制台厂商预设全面更新（agent/console_html.py PROVIDERS）：
   DeepSeek（+v3.2）、Kimi（+kimi-k3）、智谱（+glm-4.6）、通义（+qwen3-max/plus/235b/32b）、MiniMax（+M2）、
   ChatGPT（+gpt-5/gpt-5-mini/o3/o4-mini）、Claude（+opus-4-1/sonnet-4-5）、Gemini（+gemini-3-pro-preview）、
   新增 Grok（xAI：api.x.ai/v1，xai- 开头 Key，grok-4/3/3-mini/2）——切换/弹 Key/按厂商存 provider_keys 逻辑不变。
2. 价目表同步：新模型单价与每轮成本/10 元轮数（海外按 $1≈¥7.2；官方价会变，以官网为准，可用 model_prices 覆盖）。
3. README/使用说明（10 家厂商 60+ 型号 文案）；About/自检清单同步。
4. 端点核查：DeepSeek/Kimi/Claude 401/403=协议正确；OpenAI/Gemini/Grok 在无代理网络下不可达（用户侧开代理即可，端点为官方 OpenAI 兼容地址）。
用处：主流模型一网打尽，加 Grok；新模型价格可直接比对。

2026-09-06　离线包补微信安装包 + 依赖已装自动跳过 + 微信卡死守护
改动：
1. 离线包 offline\ 新增 wechat\：微信 4.1.13 安装包（WeChatWin_4.1.13.exe，244MB，版权归腾讯，仅供离线安装；配套说明文档同步）。
2. 安装依赖.bat 改为"先体检再装"：调用 scripts\setup_deps.py —— 14 项依赖版本体检，全部就绪且版本正确 → 直接跳过（提示下一步自检），缺什么装什么（离线/联网自动识别），装完自动复查；bat 本体改为纯 ASCII（根治中文乱码）。
3. 微信卡死守护（_wechat_watchdog）：每 30 秒检测微信主窗口响应（SendMessageTimeout WM_NULL），连续 2 次无响应＝卡死 → 自动 taskkill 微信 → 2 秒后重新拉起 → 弹窗「请重新登录」→ 写日志；微信恢复后机器人轮询自动继续，无需重启机器人。
4. 说明按现状修正：README（安装步骤/离线部署含微信安装包/微信守护）、使用说明同步。
用处：离线部署一步到位（含微信本体）；已装好的机器一键跳过不再重复安装；微信卡死不再需要人盯，自动重启并提醒登录。

2026-09-06　收尾整理：删除「检查微信版本.bat」+ 说明修正（自检 51 项）
改动：
1. 根目录「检查微信版本.bat」删除（UTF-8 中文被 cmd 按 GBK 读会乱码报错；且启动已自动体检+弹窗询问修正，脚本冗余）。版本体检能力保留：启动自动体检、控制台体检结果行、scripts\wechat_check.py 可随时手动运行（py -3 -X utf8 scripts\wechat_check.py，--update 自动修正）。
2. 使用说明自检项数修正为 51 项；README 体检说明补充。
用处：根目录只剩必要文件；说明与实际 51 项自检一致。

2026-09-06　启动自动版本体检 + 不匹配自动弹窗询问修正（免手动点检查脚本）
改动：
1. 启动（双击 启动机器人.vbs / pythonw）时**自动体检**：微信版本（4.x）、适配层 wechatauto-replica（1.1.5.1）、14 项关键依赖最低版本——全部匹配则静默继续，无感。
2. 存在不匹配 → **弹窗列出明细并询问"是否现在自动升级修正？"**：选「是」自动 pip install -U 适配层+全部关键依赖并报告结果（提示重启）；选「否」以当前环境启动。命令行启动时只打印日志不弹窗。
3. 控制台「体检与功能自检」新增版本体检结果行（✅ 匹配 / ⚠️ 不匹配，实时）；状态接口新增 dep_ok。
4. 「检查微信版本.bat --update」保留为手动模式（批量/无弹窗场景）。
用处：微信升级/换电脑后启动即体检，不匹配会主动问"要不要修"——不用记着去点脚本。

2026-09-06　版本体检增强：关键依赖逐包校验 + --update 一键自动修正
改动：
1. 「检查微信版本.bat」升级为**版本体检**：微信本体（≥4.x）+ 适配层 wechatauto-replica（必须 1.1.5.1，与微信版本匹配）+ Python 版本 + **14 项关键依赖**（requirements 最低版本自动校验：psutil/uiautomation/pywin32/zstandard/Pillow/requests/urllib3/cryptography/pyperclip/colorama/winsdk/imageio-ffmpeg），全部 OK 才算"版本匹配"。
2. 加参数 `--update`：自动升级 wechatauto-replica 与全部关键依赖（pip install -U），实现"检测到版本不对 → 自动更新修正"；微信本体升级仍走官网（不会自动替换）。
3. 自检（一键体检）新增「关键依赖版本匹配」项，共 51 项；版本校验函数 agent.wechat.dep_check 供自检/检查脚本/后续复用。
4. 已适配到"版本不对就跑不了"的全部点：微信本体（UI/DB 结构）、适配层（随微信 UI 升级）、Python（离线 cp310 提示）、关键 pip 依赖（最低版本）；数据库可读性原有自检项继续覆盖。
用处：换电脑/升级微信后一条命令体检+自动修正，杜绝"版本不对莫名其妙跑不了"。

2026-09-06　微信版本自动检测 + 适配层一键检查（微信更新后不再"莫名不能用"）
改动：
1. 新增微信版本检测（agent.wechat.wechat_version_info）：自动读取微信进程版本（Weixin.exe，如 4.1.13.63）与适配层 wechatauto-replica 版本，判定是否支持（微信 4.x+）。
2. 接入四处：启动日志；控制台「微信」卡片（显示 微信版本 · 适配层版本，低于 4.0 红字警告）；自检新增两项（wechat 导入 + 微信版本检测，共 49 项）；状态接口 /api/status。
3. 新增根目录「检查微信版本.bat」+ scripts\wechat_check.py：一键查看 微信版本/路径/适配层/支持结论/异常怎么办；参数 --update 自动升级适配层（pip install -U wechatauto-replica）——微信官方更新界面后点击类功能异常时的标准处理，不需要重装微信；微信本体升级请从官网下载。
4. 文档更新：README 运维节/目录节 + 使用说明新增 FAQ。
用处：微信升级或版本不对时，一眼看出差距并一键更新适配层，不再"莫名其妙就不能用"。

2026-09-06　模型厂商新增国外三家（ChatGPT/Claude/Gemini）+ GitHub Release v2.0.0 + 仓库瘦身
改动：
1. 控制台「模型厂商」预设新增 3 家海外厂商：ChatGPT（OpenAI，api.openai.com/v1，gpt-4o/4.1/o3-mini 等）、Claude（Anthropic 官方 OpenAI 兼容端点 api.anthropic.com/v1，claude-sonnet-4/3-7 等）、Gemini（Google generativelanguage.googleapis.com/v1beta/openai，gemini-2.5/2.0 等）——与既有厂商完全一致：切换自动换 Base URL、弹窗填 Key、按厂商存 provider_keys、模型列表现场切换（提示：受限网络下部分国外端点可能连不上，填真实 Key 后走控制台「测试 API 连通」验证）。
2. GitHub：Release v1.0.0（旧"初始发布"）已删除（tag 一并删除），新建 v2.0.0（Latest）为多模型完整描述；仓库 About 描述同步更新。
3. 仓库瘦身：whale-widget 上游留档文件（lib-index.js/README-原版/whale-widget-prompt/cordis.patch/package.json）移出仓库到本地 archive（运行时只依赖 client/widget.js + assets；LICENSE-原版.txt 保留合规）；README 补「目录结构与文件用途」，删除全部装饰性 ══ 分隔线（避免误读）；使用说明/更新日志同样去除分隔线。
4. 凭据：GitHub 授权（系统凭据内的 gho_ token）已写入桌面 MY apq.txt 供后续调用（敏感，勿外传/勿上传）。
用处：支持全部主流模型厂商；发布物干净、GitHub 主页文件与说明一一对应。


2026-09-06　说明按多模型现状更新 + GitHub 描述/Release 更新（gh CLI）
改动：
1. README/使用说明更新为"多模型"表述：不再只提 DeepSeek —— DeepSeek / Kimi / 智谱 / 通义 / MiniMax / 豆包 / 自定义 BaseURL，控制台厂商预设 + 按厂商存 Key（api.provider_keys）；相应改：接口必填项、系统要求（能访问大模型 API）、成本价目（未知型号按主流档估算）。
2. 通过下载的 gh CLI（+系统里已有的 GitHub 授权）完成：GitHub 仓库 About 描述更新为多模型文案；Release v1.0.0 描述重写为多模型版（含部署/功能/风险提示）；v1.0.0 tag 已指向最新代码（此前指向 11:31 的旧"初始版本"）。
用处：仓库与说明与实际功能一致；Release 可对外分发。


2026-09-06　打包归档：发布离线包 + 精简归档 + 隐私清理 + 本地数据清零
改动：
1. GitHub 仓库随包发布离线部署文件（offline\wheels 全部依赖 + 绿色版 Python 3.10，约 73MB）；微信 4.x 安装包因腾讯版权不随包，请自行安装；README 增补离线部署说明。
2. 精简归档：删除 webui.py 旧版界面模板死代码（380 行）；功能重叠的启动/停止 .bat 与 看门狗.ps1 移到 WX-chatbot\archive（不再随项目分发）；README/使用说明精简（删上游链接细节、重复段落、长清单），启动入口统一为「启动机器人.vbs」。
3. 隐私检查：config.json（API Key/控制台口令）全程 gitignore 未入库，仓库历史中也没有 data/logs/media；全库扫描无 sk-密钥/口令/wxid/群名——可放心分发。
4. 本地数据清零（机器人变身"全新"）：删除 data 下 记忆/聊天存档/会话明细/挂件账单 与 logs/media，旧数据备份在 WX-chatbot\archive\wx-agent-旧数据-20260906。
用处：包可直接分发给他人（离线可装）；仓库干净无隐私；本机机器人从零开始。


2026-09-06　气泡点击点移到气泡中带（容错提升）+ 程序级引用规则（新对话开始 70% 引用对方第一句）
改动：
1. 气泡点击点：从气泡左缘（头像+70）移到**气泡中带**（头像中心+130 = 左缘再往右 60px），更靠近气泡中心、避开圆角/内边距，右键触发菜单更稳；候选序列 [中带(OCR行中心y) → 中带(ay+30) → 左缘(OCR行中心y) → 左缘偏右] 依次尝试，任一弹出菜单即成功（短气泡自动回退左缘）。
## 2. 引用时机规则（程序级，不依赖模型自觉，可在控制台「发送限制」调整）：
   - 触发：机器人在该群**上一条消息超过 300 秒**（对话已冷场）→ 判定为"新一段对话开始"；
   - 概率：默认 **70%** 自动引用**对方最近的一句话**（消息文本+发送者名定位引用）；
   - 配置：send.quote_on_new_talk（开关，默认 true）/ send.quote_reply_probability（概率，默认 0.7）/ send.quote_new_talk_gap_s（冷却秒数，默认 300）；
   - 模型显式指定回复哪条（reply_to_message_id）时以模型为准；连续对话中（机器人刚发过消息）不触发，避免每条都引用。
## 3. 控制台「发送限制」新增三项：新对话自动引用开关、引用概率、对话冷却秒数。
用处：引用更有"真人接话"的语感（闲了一段时间后，@机器人回话自动带上引用框）；气泡点击成功率更高。


2026-09-06　修复：周期用量误清零 + 拍一拍/引用「无头像折叠组」改名走气泡、OCR 误报菜单 + UI 操作全局串行锁
改动：
1. 周期用量误清零修复：UsageStats 加载时用字符串比较判断周期是否切换，同一周期（相等）被误判为"已结束"而把本周用量清零归档——每次重启都发生。改为 >= 判定，本周用量（¥0.0225 · 29566 tok）重启后保留。
2. 拍一拍新路径：连续消息会折叠头像（消息过多时不显示对方头像）——此前"头像彩色检测失败→固定偏移点击"会点到 ID/头像间隙并生成错误结论。现在：彩色头像检测到 → 右键头像；未检测到 → **改右键气泡**（消息右键菜单同样含「拍一拍」，拍的是该消息发送者，不会误拍），并沿消息列表向上滚动查找后再定位。
3. OCR 误报菜单修复：拍一拍检测的 OCR 兜底之前无过滤——聊天文本「@E 第二条：拍一拍拍不上…」被当成菜单项，输出"✅ 菜单可弹出（识别到「@E第二条：拍一」）"。现在严格过滤：含 @、长于 12 字、行高 >46px（消息行高）一律剔除，只认精确的「拍一拍/引用/回复/转发」小字条。
4. 引用候选点重排：气泡 OCR 点（最准，无头像折叠组也适用）优先，其次 头像+70/+95；光标不再在 ID 与气泡间隙反复试探。
5. UI 操作全局串行锁：WeChatAdapter 的 _send_lock 已定义但从没使用——发消息/引用/拍一拍/自动回拍/诊断并发碰微信窗口可能"打架"（内容串台/输入框互踩）。现在每个碰微信窗口的入口（send_text/send_text_at/send_image/reply_quote/send_poke/_verify_poke_menu）统一持同一把锁串行执行；「程序做操作 vs 模型同时回答」不再并发抢窗口。
6. 控制台周期卡：无消耗时显示「—（暂无消耗）」而非 0.0000，避免误读。
用处：拍一拍/引用在消息多、头像折叠的群里也能正确工作；不再有"假成功"提示；微信窗口操作全程串行防打架；本周用量重启后不再丢失。


2026-09-06　引用交互修正（右键气泡/不乱点）+ 成本按价目表估算+按周期重置 + 运行明细（思考/token/工具）
改动：
1. 引用交互按实测修正：只右键「气泡文本」（头像中心 +70px 起），绝不点 ID/头像与气泡之间的空白；定位不到目标时**向上翻页搜索**（滚轮方向已修正、光标先移到消息区中央），仍找不到就如实失败——**不再有任何"乱点兜底"**（曾点到侧栏群名称/空白）。菜单二次右键重试保留；Esc 仅在 UIA 确认到真实菜单节点时才按（防误关微信窗口）。
2. 估算成本修复：未知/实验型号（如 deepseek-v4-flash-vision-exp）此前单价为 0 → 成本恒 0.0000；现按主流档位兜底估算（¥2/百万 in、¥8/百万 out、¥0.2 缓存，config api.model_prices 可精确覆盖），39409 tokens ≈ ¥0.135。
3. 用量统计持久化 + 按周期重置（config stats.period：daily/weekly/monthly，默认 weekly）：累计永远保留；周期用量到点归档进历史（最近 24 期）；控制台概览新增「今日/本周/本月」用量卡（价格/token/会话/条数），重启后统计不丢。
4. 新增「运行明细」分区（nav 第 3 项）：每轮对话展示 群名/状态/耗时/tokens/成本/工具调用（名称徽章）/回复摘要，思考过程（模型返回的 reasoning_content 等字段）可展开；服务端按天 JSONL 落盘（data/sessions/），新接口 /api/sessions。
5. 运行日志每行立即落盘（上一轮已加 flush），本轮确认日志文件完整可查。
用处：引用不再"说引用偏不引用"也不再乱点；成本真实可估算并按周期记账；能看清机器人每一轮在想什么、花了多少 token、调了什么工具。


2026-09-06　引用链路重做（回归根因修复）+ 拍一拍逻辑按需求重调 + 体检点击实测改为同链路
改动：
1. 引用（引用回复）回归根因：微信 4.x UIA 消息行矩形是**全宽行**，行中央多半是空白 → 右键不弹菜单 → 引用失败静默退回普通发送（表现=「不会引用了」）。修复：
   - 改用与拍一拍相同的「头像+气泡」定位：气泡文本起点 ≈ 头像中心 +70px（实测），多点命中测试；
   - 修复 _uia_target_row_rect 翻页后的居中滚轮**方向反**（目标行在顶部时应往历史滚让它回到视野中部，原代码反向把它滚出视口——也是拍一拍翻页时偶发失败的原因）；
   - 进入引用模式后不再依赖像素探测输入框（引用模式探测常失败）→ 优先 UIA 直进输入框（粘贴+回车+读回清空验证），失败再走固定矩形/完整探测；
   - 右键菜单增加「第一次只聚焦不弹菜单」的二次重试；sender 层引用失败改记日志（之前静默无法排查）。
## 2. 拍一拍逻辑按需求重调：
   - 别人拍一拍机器人 → **系统自动回拍，概率 90%**（config poke.reply_probability），同一人 30 分钟冷却（poke.cooldown_seconds），延迟 ~18 秒等先自然回复再拍；
   - 「偶尔皮一下拍别人」概率调低：主动拍（playful）受 **10% 概率 + 每天最多 3 次**（poke.active_probability / active_daily_limit），被拦时如实说没拍；
   - 群友明确要求拍某人（request）不受门控；send_poke 新增 reason 参数（reply/request/playful）供模型按规则选择；提示词同步更新；
   - 被拍事件用 patinfo 里的 wxid 直接回拍，避免同名误拍；机器人自己的拍一拍事件不回拍。
3. 一键体检「适配·点击实测」改为复用拍一拍检测的 verify-only 同链路（实测拍一拍检测可用，体检结论与之保持一致），不再右键整行中央。
4. 日志落盘：每行立即 flush（pythonw 被强杀不丢日志），排查问题不再只靠控制台环形缓冲。
用处：引用恢复可用且不再静默失败；回拍有确定性（90%）、主动拍一拍不再突兀；体检与拍一拍结论一致。


2026-09-06　修复：控制台停止后看门狗复活弹新控制台 + Logo 弹跳从底边飞出
改动：
1. 停止按钮（/api/shutdown）：现在会先按 PID 文件结束看门狗（wmic 按命令行回退）再退出机器人——之前只杀机器人自己，看门狗 5 秒后又把它拉起，表现为「页面显示已停止，却又弹出一个新控制台/新标签页」。
2. 重启按钮（/api/restart）：同样先杀旧看门狗（防双实例），再用 pythonw 无窗口拉起新看门狗接管新机器人（与 启动机器人.vbs 同一链路）。
3. Logo 弹跳：物理画布从 160×118 放大到 200×140（显示 100×70，顶栏图标更大更清晰）；鲸鱼静止位置从「贴画布底边」改为垂直居中落脚（上 74px/下 8px）；跳起冲量 620→360，跳高收进画布内，不再出现「从下面飞出框外再落下」；顺带修复 drawStatic() 无参调用 squash=undefined 导致首帧 NaN 不显示的隐患。
用处：停止=真停（不会再诈尸弹新控制台）；弹跳变成「原地 Q 弹」的小动画，鲸鱼始终完整可见。


2026-09-06　彻底去 PowerShell：启动/停止全改 pythonw + PID 文件；VBS 编码修复
改动：
1. 答案：项目运行本身【完全不依赖 PowerShell】（只依赖 Python）。之前只是用 PowerShell 写看门狗/停止脚本，才会看到 PowerShell 字样/黑框。
2. 新方案（零 PowerShell、零黑框）：scripts\watchdog.py（pythonw 隐藏运行的看门狗，崩溃 5 秒后自动拉起，等价旧 看门狗.ps1）+ scripts\stop_bot.py（按 PID 文件安静结束机器人+看门狗）。
3. 机器人启动时写 data\bot.pid、看门狗写 data\watchdog.pid，停止器读 PID 用 taskkill 结束——不再扫进程名、不再调 PowerShell/Cmd。
4. 修复「启动机器人.vbs / 停止机器人.vbs 运行不了」：旧 VBS 用 UTF-8 存中文路径/注释，Windows Script Host 按 ANSI(GBK) 读导致乱码报错。现改为纯 ASCII 内容（免编码问题）+ 路径全英文（scripts\watchdog.py、scripts\stop_bot.py）；找不到 pyw 时依次回退 pythonw、python（隐藏模式）。
5. 修复 scripts\启动机器人.bat 调「scripts\启动机器人.vbs」（不存在）→ 改为调根目录 VBS；根目录旧版 停止机器人.bat（内嵌 PowerShell）已替换为 VBS 包装。
6. 开机自启改为 schtasks 直接注册 wscript.exe 启动 VBS（登录零闪烁）；拍一拍诊断不再受白名单限制（手动诊断任意已发现群，自动拍一拍仍只拍监控群）。
用处：双击启动/停止全程无任何命令行窗口；旧 VBS 报错根除；诊断按钮在非白名单群也能用。


2026-09-06　按 grill-me 共识落地：Key 安全 + 记忆页面 + 部署向导 + 拍一拍低功率
改动：
1. Key 安全：控制台只回传打码值（sk-***…尾4位），真实密钥只存服务器 config.json；保存时打码值不覆盖真实密钥；按厂商存 api.provider_keys（切换自动回填/保存）；日志与运行缓冲统一脱敏（sk-* → sk-***，含 Bearer 形式）。
2. 记忆页面：控制台新增「记忆」分区——检测有印象的群 → 选群 → 成员印象列表（数量/更新时间/删除）；配套 /api/memory（GET 列表、POST 删除）。
3. 部署向导：首次运行（Key 为空）→ 三步向导：①填 API Key → ②检测微信群聊并勾选白名单 → ③一键体检 → 完成（可跳过）。
4. 拍一拍低功率：体检「点击实测」连续失败 2 次 → ui.poke_degraded=true，此后拍一拍直接如实说“环境受限拍不了”（不空耗 30 秒）；体检通过自动解除。
5. 模型厂商预设补齐 5 家国产厂商（Kimi/智谱/通义/MiniMax/豆包）+ 自定义，切换必弹对应 Key 输入并回填已存 Key。
用处：完成「新电脑 10 分钟内向导+体检全绿」闭环；密钥不外露、记忆可管理、拍一拍失败不空耗。


2026-09-06　Web 控制台全新蓝白界面 + 无窗口启动 + 一站式配置
改动：
1. 界面重做（参考图样式 + DeepSeek 品牌蓝 #4D6BFE 设计）：浅灰蓝底白卡片、顶部状态栏（运行/模型/余额/今日用 + 暂停/停止/重启）、左侧分区导航、右侧内容卡片；所有配置项通用 data-cfg 双向映射。
2. 设置全部搬进控制台：模型 API（密钥/模型/温度/轮次/超时/单价）、微信（昵称/轮询/白名单/限发）、人设与响应（角色文本/参与度/档位/关键词/上下文条数）、发送限制（间隔/限频/切分）、记忆、联网搜索、服务器（地址/端口/口令）、界面适配（缩放/清遮挡）——改配置不用再碰 config.json。
3. 首次运行引导：API Key 为空时全屏引导页（粘贴密钥 → 保存并测试 → 完成）。
4. 无窗口启动：启动机器人.bat 改为隐藏窗口运行看门狗 + pythonw 后台跑机器人（不再弹黑色 cmd 窗口）；控制台新增「重启」按钮（后台无窗口拉起新实例接替）。
5. 旧界面模板降级为无操作字符串保留（实际界面在 agent/console_html.py，换皮肤只改一个文件）。
用处：一站式管理所有数据；换电脑/首次部署不再手改文件；桌面清爽（无黑窗口）。

2026-09-06　拍一拍功能完成（实测可拍）+ 小鲸鱼余额挂件 + 界面适配层
改动：

一、拍一拍（send_poke，本次实测通过）
1. 定位：微信 4.x 消息列表在 UIA 树里可枚举（每行 Name 即消息原文），按目标最近一条消息精确匹配行（防截断：前 24 字模糊）；随后用「彩色像素斑块检测」找头像中心（真人头像有彩色，气泡/背景低饱和度，深浅色主题通用）。
2. 菜单：右键头像后优先查 UIA 菜单树（mmui::XMenuView，Invoke 点击零漂移）；OCR 兜底只认光标右下方向 ±320px 内、高 <46px 的小字条——聊天文本里的「拍一拍」不会再误报为菜单项。
3. 验证：点击前记录消息序号，点击后回读数据库**轮询最多 10 秒**（微信落库有 2~6 秒延迟），只认「新出现的拍一拍事件」；没验证到如实反馈，绝不假报成功。
4. 修掉两个经典误报：①假报成功（验证跳过默认成功分支）；②深色主题下 wechatauto 白色像素检测误报「锁屏/不可见」（改用自己的前置清理：最小化遮挡 + 置顶）。
## 5. 提示词规则：send_poke 失败必须如实说「没拍上」，不能说拍到了。
6. 控制台新增「拍一拍诊断」按钮：完整执行 定位→右键→点菜单→验证 并输出每一步结果，卡在哪一步一目了然。
限制：对方最近发言需在可见消息区；合成右键依赖微信版本与系统输入状态（正常桌面会话 + 重启后可用，已在实测验证）。

二、小鲸鱼余额挂件（完整迁移 DeepSeek-Balance-Whale-Widget 0.2.10，MIT 许可）
1. 原项目整体放入 whale-widget/ 留档（lib-index.js、README-原版.md、whale-widget-prompt.md、LICENSE-原版.txt、package.json、cordis.patch.yml），浏览器侧脚本原样提取为 whale-widget/client/widget.js（零改动）。
2. 功能全部落地：右下角常驻小鲸鱼、余额 60 秒自动刷新 + 点击手动刷新 + 数字滚动动画、今日已用、每轮对话消耗泡泡、拖拽 + 四边四分之一吸附 + 左吸附镜像翻转、按压 Q 弹、汉堡菜单（大小 0.6–2.5 倍 / 小黄鸭和音效1 / 音量 / 气泡开关 / 每轮消耗开关与自动关闭秒数 / 避让滚动条）、按压/松手音效、随机台词（含 rua.gif）、随窗口缩放。
3. 「今日已用」适配：直接用每次 API 调用的真实 token 按峰谷定价折算记账（工作日 9–12/14–18 高峰、2026-08-23 起周末全天谷价），每日账本跨天归零、保留 30 天，存 data/whale-state.json。
4. 「每轮对话消耗」适配：一次唤醒（一次完整回复）结束时结算本轮所有调用成本；挂件轮询 last-turn.json 弹出「上一轮对话消耗: ¥xx」。
5. 服务端按原版 /dsh-whale/* 接口约定在 agent/whale.py 用 Python 重实现（balance.json / size.json GET+PUT / last-turn.json / image.png / rua.gif / sound/*.mp3 / widget.js），前端脚本由服务端注入口令后下发。
## 6. 移除旧的迷你挂件（圆形小图标 + 悬停白框）。
限制：「实时·令牌」用量模式不接入（wx-agent 用自己更准的记账）；今日已用从本次更新起记账。

三、界面适配层（agent/ui_adapt.py，跨电脑通用）
1. DPI 缩放：自动检测（物理分辨率 ÷ 进程感知分辨率，对 100/125/150/200% 都正确），点击坐标自动换算；控制台可手动指定档位（auto/1.0/1.25/1.5/1.75/2.0）。
2. 遮挡清理：点击前自动关闭 Windows 手写输入画布等系统叠加层（TabTip/ShellHandwritingCanvas）、最小化遮挡窗口；并校验「点击点当前是不是微信接收」——被挡时如实报告是哪个窗口挡的。
3. 全功能接入：wechatauto 实例的 wx_click / ensure_visible 替换为适配层版本，拍一拍、引用、@、发图、发送按钮回退、open_chat 回退全部生效。
4. 配置：ui.coord_scale（auto 或数字）、ui.clean_overlays（默认 true）。
限制：微信 4.x 右键菜单等「自绘、吃真实鼠标事件」的交互，在系统输入状态异常（远程会话/输入卡顿）时会不可用；建议双击 scripts\启动机器人.bat 在真实桌面会话运行。
自检：47/47 通过（新增 agent.ui_adapt）。


2026-09-06　修复自问自答死循环 + 引用图片识图 + 控制台鲸鱼挂件/停止按钮/图标
改动：
1. 修复死循环：微信 4.x 群聊里机器人自己的消息 real_sender_id 是"3"（不是"2"），导致把自己的话当成"别人复读"。现在跳过 sender_id 为 2 或 3 的消息。
2. 修复识图：用户"引用图片 + 发文字"会存成一条 zstd 压缩的引用消息（类型被当成"文件/链接/卡片"）。现在会解压并解析 <refermsg>，反查出被引用图片的 local_id，送给视觉模型看图。
3. Web 控制台新增「停止机器人」按钮（调用 /api/shutdown 一键退出进程）。
4. Web 控制台新增「鲸鱼计费挂件」：右下角浮动小鲸鱼显示余额，点击刷新；顶栏加开关控制显示/隐藏。
5. 图标：库和 Web 控制台统一使用 assets/icon.png（用户提供的图片），并作为浏览器 favicon。
6. 修复「拍一拍」没反应：拍一拍是 zstd 压缩的 appmsg（type 62），标题形如「E」拍拍「群deepseek」；之前当成普通文件卡片忽略了。现在解析出拍的人，转成 [拍一拍]（某人）喂给模型。
7. 修复「查看原始 JSON」报 401：该链接改成按钮，打开时自动带上访问口令。
8. favicon 从 <body> 移到 <head>（之前位置不对，浏览器可能不认）。
用处：机器人不再刷屏、能看懂引用图片、能回应拍一拍；控制台能一键停止、有余额挂件和自定义图标。

2026-09-06　修复两个 bug：控制台 401 + 自问自答死循环
改动：
1. 修复 Web 控制台「测试 API」等接口报 401：加了随机口令后，前端 JS 没把口令带上。现在页面会自动从网址 ?token= 里取口令，所有请求带 Authorization 头。
2. 修复机器人把自己发的消息当成别人、导致 1 秒连发 6 条的死循环：群聊里 sender_id 不可靠，改用双重过滤——① 发送者 wxid == 机器人自己 wxid 就跳过；② 新增"回声过滤"（记录自己刚发的文本，30 秒内回读到完全一样的文本就跳过）。
用处：控制台能正常用；机器人不会再自问自答刷屏。
备注：本次自检 45/45 全部通过（API Key 已填）。

2026-09-06　
改动：

1. 新增 .gitignore：忽略 config.json（含 API Key）、data/logs/media/**pycache**/offline 等敏感与运行时文件。
2. 新增 config.example.json：配置模板（Key 和口令留空），供克隆者复制改名后填写，避免泄露真实密钥。
3. 新增 LICENSE：MIT 许可，并附上游项目许可说明（qq-agent 等版权归原作者）。
4. README 与使用说明：把「风险警告」和「快速安装」提到最前面；补了 config.example.json 的使用说明；修正目录结构和过期的本地路径。

2026-09-06　拍完验证 + 控制台随机口令与自动开浏览器 + 修复测试API
改动：

1. 拍完验证：send\_poke 点完「拍一拍」后回读数据库，确认是否真出现了「拍了拍」事件，把「已验证 / 未验证到」如实反馈给模型，避免"自以为拍成功"的误判。
2. 控制台随机口令：server.token 留空时启动自动生成 48 位随机口令并写回 config.json；控制台网址变成 http://127.0.0.1:3210/?token=xxxx（又长又复杂，别人猜不到、也防端口撞车）。
3. 自动开浏览器：server.auto\_open\_browser 默认 true，启动后自动用默认浏览器打开控制台（带口令的网址）。
4. 修复 bug：test\_api\_fn 和记忆整理里把消息错传成了字典而非列表，导致「测试 API」和「记忆整理」请求 400 报错——已改为正确的列表传参。
5. 实测通过：真实 API 打通（test-api 返回 pong、延迟 917ms），余额查询返回真实余额，口令鉴权 401/200 正确。
用处：拍一拍结果可验证；控制台更安全；启动即可在浏览器里管理；测试 API 和记忆整理恢复正常。

2026-09-06　连发多条确认 + 新增发图与拍一拍 + 引用
改动：

1. 确认「连发多条」早已支持：send\_message 传字符串数组即可分条连发（每条间隔真人化随机延迟）。
2. 新增 send\_image 工具：把某条消息里的图片转发/发送到当前聊天（下载后经剪贴板发送）。
3. 新增 send\_poke 工具：拍一拍某位群友。按 PC 微信实际交互实现为「右键对方头像 → 菜单选『拍一拍』」（最初误做成双击头像，已按用户实测纠正）。
4. 新增「引用」：send\_message 的 reply\_to\_message\_id 现在会走「右键最近消息 → 菜单选『引用』→ 发送」，不再是无操作。
## 5. 提示词「微信场景规则」补了发图和拍一拍的用法说明。
6. 新增「感知被拍」：识别系统消息里的「拍了拍」事件，转成 \[拍一拍]（带拍的人名）喂给模型——机器人被拍时能自然回应（此前根本看不到拍一拍事件）。
用处：机器人能发文字（连发/引用/@）、能发图、能拍人、能感知被拍。
限制：微信「收藏表情包 / 从表情库发送」因表情面板是自绘 UI、无法可靠定位，暂未实现（发图用 send\_image 代替）。「引用」目前引用的是最近一条消息（近似）。
说明：顺手修复了 README.md 里残留的 53 处反斜杠转义（\_ → \_）。

2026-09-06　余额查询与成本计算
改动：

1. 新增 query\_balance() 余额查询——调用 DeepSeek 官方 /user/balance 接口，返回总余额、充值、赠送三部分，30 秒缓存。
2. 把 estimate\_cost()（成本估算）接入每次会话统计：每跑一次会话，自动按 token 用量累计花费。
3. 内置 deepseek-chat / deepseek-reasoner 官方单价表（元/百万 token），没手动填价也能算成本。
4. Web 控制台：顶栏加「余额」徽标（点击刷新、30 秒自动刷新）；状态卡加「估算成本」；新增 /api/balance 接口。
用处：
不用切到 DeepSeek 官网就能在控制台看账户还剩多少钱；机器人每回复一次，界面实时累计这次会话的花费，方便盯着成本。
（余额查询方式参考了开源项目 DeepSeek-Balance-Whale-Widget）

2026-09-06　文档转纯文本
改动：把 README.md、使用说明.md 里的 Markdown 符号（#、\*\*、反引号、表格竖线、> 等）和多余空行去掉。
用处：用记事本等纯文本编辑器打开时更清爽，不会被一堆符号干扰阅读。

2026-09-06　补充来源信息与风险提示
改动：

1. 致谢表补上两个上游项目的真实来源：微信项目（B站视频 BV1Mz4267EHQ / GitHub bdydgz114514/wechat-deepseek-bot）；QQ 项目（B站网友 Kondius 基于 qq-bridge 修改，修改版未上架 GitHub，下载地址为 bilibili 动态 t.bilibili.com/1244553403559837713）。
2. 新增「风险提示」章节：说明第三方微信机器人有封号风险，并解释本项目 UIA 无注入机制（不注入、不 Hook、不改微信文件）如何把风险降到较低水平，以及用小号、控频率等建议。
用处：交代工程出处、便于合规使用；让使用者对封号风险有清晰预期和应对办法。

2026-09-06　离线部署包
改动：新增 offline/ 目录，内置 24 个依赖 wheel（Python 3.10 版）和绿色版 Python 3.10.11 运行时；安装依赖脚本支持离线模式。
用处：目标电脑断网也能一键装好全部依赖，真正做到「拷贝即部署」。

2026-09-06　Web 控制台 + 视觉模型
改动：

1. 新增 agent/webui.py：浏览器里改设置、看状态、看日志、测试 API。
2. config 默认模型换成视觉模型 deepseek-v4-flash-vision-exp（能看图 + 联网）。
3. 修复默认人设未注入「小鲸鱼」角色卡的问题（role\_text 留空时自动用内置角色卡）。
用处：不用手改配置文件，在网页上就能管理机器人；默认支持识图；人设默认完整生效。

2026-09-06　初始版本 v1.0
改动：整合 qq-agent（智能大脑）+ wechat-deepseek-bot（微信 UIA 接入），产出主程序 wx\_agent.py、agent/ 包（14 个模块）、config.json、requirements.txt、README.md、使用说明.md、运维脚本（启动/停止/备份/开机自启/看门狗/自检/安装依赖）。
用处：把 DeepSeek 多模态大模型接进微信群，实现 @提问、识图、联网搜索、群友记忆的聊天机器人。
