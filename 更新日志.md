# 更新日志（Changelog）

按时间倒序。每条写明「日期：功能 / 修复」。

## 致谢

本作品整合自以下开源项目（经二次开发整合，仅供学习交流；上游版权归原作者，许可证随包附送）：
- QQ Agent（智能大脑）：由 B 站网友 Kondius 基于 qq-bridge（https://github.com/Derpyu520/qq-bridge）修改而来。相关 B 站视频 BV1ss8R6zERG；修改版下载地址 https://t.bilibili.com/1244553403559837713
- wechat-deepseek-bot（微信接入层，底层 wechatauto UIA 无注入）：B 站视频 BV1Mz4267EHQ，GitHub https://github.com/bdydgz114514/wechat-deepseek-bot
- DeepSeek-Balance-Whale-Widget（小鲸鱼余额挂件）

不过尽管有这几个项目珠玉在前，但是做兼容和编写新的功能仍然十分困难，最终消耗的精力几乎相当于重构。

- **角色卡评分细则 v2.0（唯一权威，百分位精度 0.01）**：五维=风格辨识25%/角色贴合30%/内在一致20%/表达自然15%/完整可用10%，每维 0~100.00；扣分上限：无口头禅→风格≤45、通用词口头禅→≤70、AI套话→表达≤65、客服口吻→贴合≤60、换角色都能用→≤50、示例占位→完整≤75、沉默类无扩展→≤70、缺说话规则→≤70；满分 100.00 唯一条件=仅凭提示词+一次提醒即逐句贴合本人（否则一律<95，优秀 88~94.99）；总分=Σ(维度×权重) 保留 2 位小数。细则也内置在控制台提示与 agent/persona_rating.py。
- **模型补足循环**：补足=人设导向（让说话更像本人，禁止围绕打分/分数调整）；每轮补足后自动按细则重评，**分数上升才进下一轮**，不升/降即停止并保留上一轮；UI 可选轮数（1~3，默认 1）；实测：自定义"高冷猫娘"卡评分 58.75 → 补足 1 轮 → **89.05**（真实上升 30.3 分）。

## 2026-09-08（vβ·Ⅱ·Ⅲ 紧急修复与全量验证）

- **⑤人设贴合度大修复（用户质疑"像不像本人"）**：
  ① **补足模板去网梗**：此前 enrich 给所有卡附加通用段（含"V我50/6/草"等当代网络梗）——古装/名著角色说话带网梗=严重不贴合。改为中性贴合式（高冷"嗯，你猜"、活泼"才不是呢"，明示"打闹也要贴合人设、不玩当代网络梗"）。
  ② **冉阿让重写**：原卡"我欠世间的慢慢还/够了吗"太通用→按雨果原著重写（苦役/主教银烛台/珂赛特/沙威替罪顶罪/终生赎罪），台词压着苦役分量，应对话术（坐过牢？/总帮别人/累吗）贴合角色。
  ③ **全面外语原名标注**（用户新规则"原名是外语的全部标"）：97+12+12 张卡补官方英文原名（原神/月计/女神异闻录/密教信徒/司辰官方 Hours 名 The Sun/Forge of Days/…；司辰中文名保留、括号注英文原名）。**原名是中文的不标**（李白/武松/赵云等）。修误标：苏芳（Philemon）→苏芳（原名未确认不标）；A/艾因（A）→A（艾因）。
  ④ **评分重跑+精排**：185 张全重评（整分仅1张）→ 精排去重 → **186 分全唯一（72.65~90.29）**。
- **⑦角色卡恢复上个人设**：人设区新增「↩ 恢复上个人设」按钮——应用新角色时自动备份上一个（cfg.persona.last_used），一键真实恢复（名字+文本），防"应用错了"。
- **⑦光标默认鲸鱼修复（真因）**：assets/cursor.png 原为 **512×512**——超出浏览器 cursor 硬上限 128px，**Chrome/Edge 直接拒载**→白箭头。重建为 **64×64 透明 PNG**（icon-whale 裁剪）。此前自定义生效（上传已缩64px）而默认不生效即此因。
- **⑦概览计费按钮**：补 type=button + 图层提权（防个别环境点击失效），位置独立行。

## 2026-09-08（vβ·Ⅱ·Ⅲ 紧急修复与全量验证）
- **⑦行为推荐接模型**：控制台「根据角色卡推荐行为档」改为**本地五维因子启发式 + 模型多维度复核**（/api/persona/behavior-recommend：参与度 low/medium/high + 表情包 0~3 + 理由；规则：极简/高冷/庄重→低参与且表情≤1，话痨/气氛组/爱玩梗→高参与且表情≥2，禁止不确定；应用后写入 cfg.persona.participation + store.sticker_level 并回读验证，**确认应用真的生效**）。
- **⑦水光波纹默认关**：ui.wave_fx.enabled 默认 False（config.py + config.json 已改）；控制台可开。
- **⑦学习评估百分位修复**：评分细则禁止整分/半整分（每维必须带非零百分位如 84.37），模型输出整分时自动重试一次并提示。
- **⑦光标自定义修复（真 bug）**：syncCursorFromCfg 使用了 IIFE 内部局部变量 CUSTOM_URL（未定义）→ ReferenceError → 整段异常 → cursor 永远不生效（白箭头）。改为全局常量 CURSOR_CUSTOM_URL/CURSOR_DEFAULT_URL；点击预览、上传保存、重置均验证。
- **⑦背景/保存**：默认背景 URL 错（/assets/ocean1.jpg 不存在，实际在 assets/wallpaper/）→ 改为 /wallpaper/ocean1.jpg（**海浪默认背景，无需先上传再恢复**）；applyCustomBg 同步修正。
- **⑦地址乱码化（默认关，可配置）**：新设置项「界面适配→地址栏乱码化」（ui.obscure_url，默认 False）；登录后一律移除 ?token=；开启时地址栏路径变随机乱码串（保护地址不被复制直接登入），服务端支持乱码单段路径也返回控制台页；端口号真实保留（浏览器必须用真端口连接，无法乱码）。修复路由判定（/api/ 不误 404）。
- **⑦新建/添加分区弹窗重构**：类型下拉按原设计；「添加角色到分区」分区名改为**下拉选单**（已建内置+用户自定义分区全列出，最后一项"✏️ 自定义…"→选它才显示自定义输入框；选已有分区自动匹配该分区描述），角色名+角色文本+创建按钮；自定义新分区时自动在"我的分区"里一并创建。
- **⑦排序按钮 WPS 式（补充要求）**：「按评估分数排序」改为**图标按钮**——点击一下正序（分数高→低），再点一下倒序（低→高），再点返回正序；图标随状态切换。

- **④涟漪 v12（按用户反馈：模块内投石入水）**：扭曲范围=**光标所在的整个模块**——顶栏→顶栏范围、导航栏→导航栏范围、功能卡→该卡范围（JS 按宿主 rect 实时设透镜 size/pos/圆角，波纹**绝不越过模块边界**）；mask 渐变中心=鼠标在模块内的位置，环带从中心一波一波向外扩散（每 ~110ms 推进相位），随着半径增大强度快速衰减（falloff 2.6），到模块边缘自然消失；无模块的空白处不显示。此前"只是把影响范围调大"的问题本质=透镜固定居中于鼠标的圆，现改为宿主矩形+鼠标相对中心。设置卡 8 参数不变（开关/强度/波速/鼠标提速/上限/半径/衰减/环数/荡速）。
- **④涟漪 v11「投石入水」+ 控制台可调**：效果改为**水中投石**——鼠标中心扭曲最强，波纹环一圈圈向外荡开、扩散到鼠标所在的整个区域，越往边缘越弱（falloff 指数默认 2.6，边缘几乎无扭曲，不影响阅读）；基础波速 5.2 rad/s + 随鼠标拖动大幅提速（增益 0.02/上限 8）。**控制台新增「🌊 水光波纹」设置卡**（导航可见）：开关 + 扭曲强度/基础波速/鼠标提速/提速上限/覆盖半径/距离衰减/扩散环数/荡开速度 共 8 项参数，改完点「应用」即时生效（不刷新页面；存 cfg.ui.wave_fx）；参数纳入 config.py 默认值。速度更快、鼠标提速加强（v10 基础）。
- **④涟漪 v10（微调）**：波光速度 2.4 → **5.2 rad/s**（更快）；**随鼠标拖动速度加快而加快**（实时测鼠标速度+指数平滑，速度增益 0.02、额外上限 8 rad/s——缓动则粼粼、快划则翻涌）；强度 14→**17**（呼吸 ±38%）；透镜 240→**258px**。v9 基础上按用户四点反馈微调。
- **④涟漪重做 v9（按用户反馈三轮迭代）**：**彻底删除水波纹圆环**（ripple 圈圈，v4/v5 两处 JS 均移除）；扭曲改为**跟随鼠标的一小圈水光透镜**——backdrop-filter(feTurbulence+feDisplacementMap) 只影响鼠标周围 240px 圆内，radial mask 边缘柔和如光晕，只影响光标所在局部、不动全屏；**滤镜参数按正弦持续流动**（baseFrequency/scale 30fps 波动，水光粼粼每时每刻在动）；卡片/侧栏/海洋背景不再整体挂滤镜（修正 v7"影响范围太大"）；逐字浮动加强（-2px→-3.5px）、卡片浮沉加强（-1.5px→-3px）、鲸鱼光斑背景漂移加强（-22px/1.04→-30px/1.06）。

## 2026-09-08（vβ·Ⅱ·Ⅲ 紧急修复与全量验证）

- **⑩朋友圈纯文字 dry 实测（真机逐屏截图通过）**：点侧栏图标→朋友圈窗出现→**长按相机 2 秒→「这一刻的想法…」弹窗**→输入「检验朋友圈：程序鼠标没问题」→**到输入框即止（不点发表/不真发）**→关窗；全程 _scratch/shots/moments_1~4.png 存证。**修复两处真 bug**：① 相机坐标原写死 (66,36) 会点到铃铛（弹「全部互动消息」）→ 改为按窗口比例（实测铃铛 0.083w、相机 0.154w、刷新 0.224w）；② 弹窗输入原走 gui.input_text 会把文字打进**主聊天框** → 改为点进「这一刻的想法」输入区 + 剪贴板 Ctrl+A/Ctrl+V 直贴 + 深色像素验证。
- **③控制台紧急修复**：`Set-Cookie` 原在 `send_response()` **之前**发送 → 响应首行变成 Set-Cookie、整条 HTTP 响应损坏（BadStatusLine，控制台/接口全挂）→ 修到 `send_response` 之后（_json 成功响应统一种 cookie；401 不种）。验证：首页 200 + cookie、带 cookie 免 token 200、无凭据 401、POST 正常。
- **⑤人设收尾（全量 3 次中位复评，169 张全 ≥75）**：单次全扫 → 6 张 <80 做 3 次中位 → 仅金克丝（62.75）真弱 → 重写（专属语癖/应对话术）→ 中位 91.2；另重写 12 张原内置弱卡（高冷猫娘/严肃班主任/段子手/肌肉男教练/咸鱼玩家/温柔姐姐/星露谷农夫/空洞骑士小骑士/夏侯惇/毒舌老哥/霸道女总裁/傲娇助手），中位 88.35~92.0。persona_fix 终评改用 score_median(3)；PERSONA_CATS 删除已删职位卡残留（librarian_wf/craftsman_wf）。
- **调试噪音清理**：emoji 分步计时 print/`_scratch/timing.log` 改为 `WX_DEBUG=1` 才输出（平时完全静默）；旧 timing.log 删除。
- **控制台**：「程序鼠标检验」新增「朋友圈：纯文字发布 dry」按钮（同后端 moments_publish）。
- **打包**：重新打包源码+离线两 zip（不含 config.json）。

## 2026-09-08（vβ·Ⅱ 全量增补）

- **版本号改为希腊+罗马**：`vβ·Ⅱ`（控制台顶栏显示 vβ·Ⅱ（构建号））。
- **①日历**：概览旁每日明细日历；选中日期高亮/描边/放大特效 + **概览各项联动当日/截止日数据**（8s 刷新尊重选中）；"本次其他工具成本"→"今日其他工具成本"。
- **②代码体检**：「代码检测＋依赖核对」真传 deps（此前空跑）、轮询 200 完成即停、**常驻进度提示**（页面加载显示状态/运行中百分比/完成留提示）。
- **③网址密码**：页面/API token 校验基础上加**会话 Cookie**（wxauth HttpOnly/SameSite=Strict/30天）+ 授权后种 Cookie + **前端自动移除地址栏 ?token=**（防他人复制 URL 登入）；访问密码=config server.token 可改。
- **④涟漪 v5**：鼠标划过周围——卡片缓慢浮动震荡(0.6s缓频)+标题/标签微浮动+背景微光；保留点击细环与逐字浮动。
- **⑤人设**：扩充到 **169 张**（女神异闻录全代主角/主角团、密教模拟器（教主/流亡者=Exile/杜弗尔=其父/长生者大敌/司辰：骄阳·白日铸炉·悼歌诗人·赤杯·飞蛾·瞳中扉·蚁母·上校·狮子匠·浪潮·制花人·树中牝马…，**全部官方中文译名**）、边狱巴士、司辰之书职位卡已删），全部"你是本名"开头+分区齐全；模型五维评分（3 次中位稳定化）；试运行评估（抽查 4 张按卡试说都贴合）；默认小鲸鱼强化（更多日常场景）。
- **⑥token 优化**：监听档位过滤（一档艾特/二档艾特+关键词/三档随机/四档全传）、历史"近 8 条详细+更早摘要"压缩、系统提示压缩、工具精简、thinking off、缓存提示（单轮约 2800~3000）。
- **⑧删本地评分**：persona_check 弃用（code_check 改用模型五维评分说明）。
- **⑨删除选删**：运行明细加勾选删除（按日期删 jsonl）+计费历史勾选清空。
- **⑩朋友圈纯文字**：长按相机2秒→输入→发表；加 **dry 验证**（到输入框即止不真发）。
- **⑪鼠标检测**：修复"单项失败就停/abort for no reason"（前端超时 30s 误杀 60-100s 检测→180s；后端单项失败不中断；常驻提示）。
- **⑫中转站**：Base URL 可直接填 API 中转站地址（降 token 花费）。
- **机器学习**：语言风格训练"确定学习/学习评估"两按钮（模型八维细则·百分位·仅评机器人发言）。
- **自检整改**：纠正神话级错误（流亡者=Exile、杜弗尔=其父；司辰官方译名；删除非真人的司辰之书职位卡）；凡未官方确认的名字不再编造。
- **打包**：`wx-agent-vβ·Ⅱ.zip`（97.4MB）+ `wx-agent-offline-vβ·Ⅱ.zip`（413.3MB），均不含 config.json。

## 2026-09-08（b.0908 增补）

- **表情面板彻底修好（真机 21/27/24/31 全对）**：滚轮方向写反修复（+wheel=顶、-wheel=底）；点击改「滚动后检测表情格中心」（表情格为固定物理尺寸 105×107px/行距 132px，不随窗口 sh 缩放，用窗口比例会点偏上一格）；处理「顶部/底部 85px 半行」与「最后一行滚到底」；滚轮步进 -150/行（-300 会一次滚 2 行导致偏一行）。全程纯程序自主判断（index→行列→滚→点格中心），非看图。
- **朋友圈点不到修复（034236）**：侧栏图标 `sidebar_items` 用标定时窗口的绝对 y（旧 render_h=1575），当前窗口不缩放导致点偏；改为按当前窗口高缩放 → 朋友圈相机图标可点、窗口成功打开。
- **表情面板发送校验修复（033444）**：配合面板方向/点中心修复 + 检验转文件传输助手，校验通过。
- **检验按钮改造（避免打扰真人）**：新增「文件传输助手」默认检验会话；`emoji_panel` 走文件传输助手；`moments_like` 只到「可点赞菜单」未实际点赞；`moments_comment` 加 dry 模式只到「可评论输入」未实际发送。
- **界面文案风格（正常/鲸语）**：确认 `ui.text_style` 保存/落盘链路正常（API 设 whale→读 whale→还原 normal 均成功）；显示切换为前端 applyWhale 时机，待浏览器复核。
- **界面刷新契约**：所有「保存设置」保存后自动刷新页面（文案风格/主题/配置立即生效）；前端请求 30 秒超时保护（服务端卡死不再"点了没反应"）
- **界面文案风格**：正常 / 🐋 鲸语（DeepSeek 味：先想再答/算力是省的感情是真的/让我先推理一下……；两种风格**功能与说明含义完全一致**；后续功能更新默认两套文案同步更新）
- **自定义背景**：上传图片（PNG/JPEG/WEBP/GIF，自动降采样 ≤1920；清洗+明确报错）→ 清晰显示（不做暗化/遮罩，毛玻璃仅在卡片与导航栏范围内）；「恢复默认」一键回滚
- **光标**：上传/重置后强制刷新并立即重绘（光标真变）；预览=实际同图
- **代码检测提速**：内联全量编译+快速依赖核对（~10~20 秒，不再 subprocess）；依赖核对=版本读取（秒级）
- **涟漪 v4（文字浮动）**：经典逐字波浪实现（每个字动画相位递增，hover 卡片标题「细浪一波波」）+ 卡片轻柔浮沉 + 光标细环；导航栏不参与动画（质感一致）；侧栏 top 88px 与顶栏保持距离
- **导入种子库**：新增「选择文件导入」按钮（txt/json 文件→自动查重→合并写入→立即生效）
- **行为推荐修正**：只分析角色本体段（排除通用规则误判）+ 关键词扩展+阈值放宽（实测：活跃/安静角色都能正确分档）
- **模型评分 / 模型补足拆分**：贴合度唯一标准=模型联网理解角色（本地规则无法判断人设，故移除「本地评分」按钮）；「模型评分」一键输出贴合分+理由（真实 LLM 实测返回 JSON）；「模型补足」联网整理角色设定重写文本（修复 scripts import）
- **自定义卡任意分区可删**（移动/删除按钮对所有自定义卡显示）；可删自定义分区（分区下卡片自动移回 📝 自定义）
- **浅蓝雾玻璃**（纯毛玻璃无划痕）+ 七彩折射；背景图清晰

## 2026-09-07（v2.0.0·Ⅱ（罗马数字）正式版）

### 功能
- **体检乱点/慢修复**：①「适配·点击实测」改为安全版（不切会话/不搜索/不翻页，只在当前前台会话验证右键菜单——彻底消除"点开头像/搜索框填服务通知/悬停 20 秒"）；② 朋友圈点赞/评论加**窗口前置+蓝点两次重试**（首击可能只聚焦）；③ 三项高风险（消息收藏/撤回/表情右键收藏）体检中改指引手测
- **停止必停修复**：停止按钮/停止机器人.vbs 现在写 `data/stopped.flag` → watchdog 见标志即退出（不再 5 秒后把程序拉起来）；一键启动/启动机器人启动时自动清标志
- **UI 修正**：光标交互改为**仅在鼠标周围产生涟漪**（沿轨迹每 90ms 一个水波环渐散，卡片本身不抖不位移）；海洋波浪改为**SVG 曲线波浪平移动画**（两条清晰海浪 + 波纹加强）+ 边缘一圈最透、中部略实的毛玻璃
- **Token 终版优化**（结合源码研究）：DeepSeek 静态前缀自动缓存（命中价≈1/8 且缓存写入免费）已最大化——系统提示保持纯静态（角色卡+规则+工具定义），动态内容全在用户消息；引导说明再压缩；历史默认降档——**已接近该架构的理论下限**（剩余成本=用户消息+模型输出，二者与功能正相关，不可再砍）
- **程序鼠标检验并入一键自检**：一键体检现在 = 环境/配置/点击 + 程序鼠标操作检验（约 40~70 秒），保留各单独按钮；文案注明耗时与真实操作提示
- 一键启动精简：vbs 末尾残留 MsgBox 提示已删除（双击后静默后台→只弹浏览器）；删除独立的「安装依赖.bat/自检.bat」（一键启动已包含依赖检查+自装+自检，消除重复入口）；README 快装改 2 步
- **程序鼠标检验区**（体检页新增，零 token）：11 项"点按钮→程序直接操控鼠标执行"的检验（朋友圈打开/关闭/点赞/评论/滚动、表情收藏(右键)/面板发送、消息收藏/撤回、窗口清理、UI 标定）——检验程序鼠标能力而非模型理解力；带搜索框过滤+滚动槽
- **朋友圈鼠标操作真机实测闭环**（接管鼠标逐 UI 验证）：打开=第一条动态详情→底部蓝点（··，(0.89W,0.938H)）→弹「♥赞|💬评论」菜单→赞/评论可用；评论自动滚出输入框（绿色）→输入→「发送」（右下角）→**实测评论已发送**（评论框关闭）；滚动→**朋友圈信息流**（多条动态+❤点赞列表+每动态蓝点）；moments_like/comment 按实测坐标改写（相对比例自适应+OCR 菜单文字），全部完成自动关窗
- **种子库扩充到 212 条**（查重：精确+72% 相似度去重——新增 121 条精选：简书「经典神回复集锦」31 条 / 新浪「弱智吧 100 句文案」65 条 / 群聊沙雕 25 条；去重 7 条）
- **机器学习复测全过**：热烈回应居首/冷场降权 ✓；系统提示纯静态（风格参考移用户消息）✓；毒舌角色+文艺种子真实 LLM 测验 → "累就对了，说明你还没躺平"（毒舌腔调，种子仅提供灵感，无左右脑互搏、无角色卡冲突）✓；高冷→安静档/斗图→表情3级 ✓
- 消息操作：collect_message（收藏一条消息）/ recall_message（撤回自己 2 分钟内消息）——复用「头像定位+气泡右键」菜单通路（与拍一拍/引用同一套坐标算法，UIA 优先 OCR 兜底）
- 朋友圈（实验性）：moments_like（OCR 找「赞」点击）/ moments_publish（发布纯文字朋友圈，找不到入口绝不误发）；入口坐标 ui.moments_entry 可配置（微信卡填写，跨电脑校准 + DPI 适配）
- 人性化行为决策引擎（agent/behavior.py，零 token 纯规则）：收到表情→概率收藏；群里有表情→概率回发收藏的表情；概率 @ 活跃成员；点赞朋友圈开关——每类行为=概率×冷却×每日上限；**人设参与度/表情包等级自动调节频率系数**（角色卡管风格、引擎管频率，解耦防刷屏）
- 角色卡行为推荐（agent/persona_hint.py，零 token 关键词）：填自定义角色卡后一键「根据角色卡推荐行为档」→ 自动建议参与度/表情包等级 → 应用（毒舌→少表情、卖萌→表情包爱好者、高冷→安静）
- 工具集扩到 22 个（表情包三件套/收藏/撤回/朋友圈/查看合并转发/记忆/搜索…）
- 人性化行为修复（实测 aa 群反馈）：用户明确要求收藏/发表情（艾特文本含「收藏/一样发」）→ **强制执行**（跳过概率与冷却，只留每日上限）；收藏支持**图片也入库**（原图下载/截图）；概率默认调高——不再出现"用户让收藏却没收藏"
- 真·微信表情路径：collect_emoji_native（右键表情气泡→菜单「添加到表情」入库微信表情库）+ emoji_panel_open/send（点输入栏笑脸→面板→爱心→点表情→回车发送）；send_emoji 优先走真实表情面板，失败回退本地收藏夹
- UI 图标库（agent/wechat_ui.py）：微信 12 个图标元素一次标定（data/ui_layout.json，像素聚类自动检测侧栏序列）→ 坐标按渲染区尺寸换算（DPI/窗口无关）→ SetCursorPos 瞬移点击 → achieve 闭环验证重试 3 次后放弃；moments_open 已接入（用户指出"点击会拖动鼠标"——wechatauto 打开会话已改 UIA Invoke 免鼠标，剩余拖动路径全部收口到瞬移+闭环）
- **Token 优化（实测系统提示从 ~10600 → ~6300 token，省 40%）**：默认小鲸鱼角色卡压缩（4500→2400 字，保留全部人设精髓去重）；系统规则 9 段合并为 5 段（删与角色卡重复的"保持主体性/该说/不该说/群聊队列/像真人/不要当管家"→合并为一段）；语言风格参考 8+8 条→3+3 条；历史默认条数降档（艾特 20→12、关键词 15→10、随机 8→6、全响应 80→30）；工具定义描述压缩（2319→1839 字符）；**单轮成本≈原来 55%**
- **UI 方案重新纠正（悬停 tooltip + 窗口标题实证）**：侧栏图标最终映射 = 头像/消息/**收藏(311)**/**朋友圈(384，悬停提示+点击弹窗标题"朋友圈"铁证)**/视频号(455)/扫一扫/小程序——**朋友圈 PC 版有入口**（此前误判"无入口"，原因是测试时残留窗口叠屏误导）；朋友圈功能恢复（打开→验证窗口→点赞/发布→关闭）
- **窗口管理逻辑链**（wechat_ui）：做完工作 → `close_leftover_windows` 枚举微信子窗口（朋友圈/视频号/收藏…）→ **点右上角叉号** → 验证已关 → 兜底 Alt+F4 → 兜底 WM_CLOSE；实测朋友圈窗口关闭验证通过（关闭后子窗口为空）
- **Token 再优化（网络方案落地：DeepSeek 前缀缓存）**：把动态"语言风格参考/高分反应"从系统提示**移入用户消息** → 系统提示（角色卡+规则+工具定义）变为**纯静态** → 每次请求前缀 100% 缓存命中（DeepSeek 缓存输入价仅为普通输入 ~1/8，长期成本再降一个量级）；另修复 hit() 中 prepare_screen 返回 bool 被 unpack 的 bug（此前图标点击全失效）
- **重大根因修复**：微信主窗被移出屏幕（render rect 负坐标）导致所有点击落空/乱点——ui_adapt.prepare_screen 增加「窗口出屏自动还原」（多屏/DPI 变化时自愈）
- 卡片水波交互：hover 时从鼠标位置荡开两道涟漪（拆 5 动作：圆环出现→扩散→内环跟进→渐平→卡片微浮+阴影加深，900ms 一次播完自动清除）；卡片透明度降到 30%（更透的毛玻璃）
- 收藏/发送表情改为**鼠标优先**：collect=右键气泡→「添加到表情」入微信表情库（本地截图仅兜底）；send=点输入栏笑脸→面板→爱心→点表情→回车（程序全程鼠标，微信原生认可）——截图证实收藏功能真实入库（全部收藏面板可见）
- **朋友圈完整鼠标操作 + 兴之所至触发**：moments_surf（刷朋友圈：滚动幅度按窗口高算 + 截图给模型看→模型决定赞/评/继续刷/结束）、moments_like（蓝点→赞）、moments_comment（蓝点→评论→输入→发送）、moments_publish_text（**长按相机 2 秒→纯文字输入栏→点「发表」**，按 UI 实测实现）、moments_scroll（滚轮，幅度按窗口高度 55%/屏）——全部操作完自动关窗；行为引擎参数（刷/赞/评/发 概率+冷却+每日上限，默认全关）在控制台调试区可调（截图耗 token，频率保守）
- **窗口关闭逻辑链实证**：朋友圈（标题"朋友圈"）与视频号（标题"Weixin"小窗）均可用"点右上角叉号→验证→Alt+F4→WM_CLOSE"关闭（实测通过）——任何微信非主窗子窗口做完工作即关
- **"显示不下"巡检（全项目）**：表情包收藏夹加**搜索框+滚动槽+60 个预览上限**；记忆印象表加滚动容器；自绘下拉/运行明细/群选择已有滚动搜索——以后同类场景一律滚轮+搜索设计
- 关窗链补充：moments 系列任何失败路径也会先关窗再报错（不留残留窗口）
- 鲸落主题再重做（第 2 版）：**清澈天蓝水色**（浅蓝渐变→淡紫）+ **毛玻璃**（卡片半透 42%、blur 18px，能看到背景）+ **琉璃斜纹质感**（卡片/背景均有斜向细纹+波纹光）+ **七彩折射**（白/青/绿/紫/粉/金光斑 + 棱镜 conic 带）；修媒体查询 bug（`not([data-theme])` 才跟随系统，不再把 whale 覆盖成深色）；背景与卡片透明度区分（背景纯渐变、卡片毛玻璃）；与「深色」主题彻底拉开
- 布局修复：左侧导航 **sticky 固定**（滚动内容时导航一直可见）+ 滚动高亮蓝色指示条（IntersectionObserver 已有逻辑随 sticky 生效）
- 「调试·高级功能」改为**真可调项**（删说明文字）：行为引擎完整参数（收藏/回发/@/点赞的冷却秒+每日上限+开关）、微信 UI 图标库布局状态、学习机制说明
- 操作可见性：人性化动作（收藏/回发表情）写入控制台「运行明细」+ 日志；收藏/撤回定位改为**只滚到底一次**（不再来回滚动翻页）；send_emoji 优先真实表情面板（笑脸→爱心→点表情），失败回退本地收藏夹并如实报错
- 说明文简化：README「装好就能用 3 步」零基础版
- 微信接入：UIA 无注入（读本地库 + 模拟键鼠），多群轮询、目标群白名单、响应档位 1~4、发送限频与真人化间隔、新对话开始 70% 自动引用对方最近一句
- 多模型：15 家厂商 160+ 型号（DeepSeek、Kimi、智谱 GLM、通义 Qwen、MiniMax、小米 MiMo、腾讯混元、百度文心、豆包、OpenAI、Claude、Gemini、Grok、NVIDIA、OpenRouter + 自定义）；控制台一键切换、按厂商存 Key、切换回填；价目表合并 QQ-agent 139 模型官方/参考价，控制台目录同步扩到 160+
- 拍一拍：右键头像/气泡双路径、自动回拍（90% + 30 分钟冷却）、主动皮一下低频（10% + 每日 3 次）、数据库验证
- 识图（多模态）/ 发图 / 联网搜索（多引擎 + SSRF 防护）/ 群友长期记忆（自动整理）
- Web 控制台：一键体检（53 项）、拍一拍诊断、运行明细（token/推理/工具/成本）、今日/本周/累计用量与成本、小鲸鱼余额挂件、三步首次向导（Key→选群→体检）
- 无窗口运行：pythonw 看门狗（崩溃自动重启 + 崩溃日志落盘）、停止器按 PID 静默结束、开机自启、零 PowerShell
- 版本体检：启动自动检测微信/适配层/依赖，不匹配弹窗询问自动修正；微信卡死守护（30 秒心跳，卡死自动重启并提醒登录）
- 离线部署包：依赖 wheels + 绿色版 Python 3.10 + 微信 4.1.13 安装包
- 群管理（按群策略）：默认 2 档响应（省 token 又够活跃）；**按群单独设档**（unified_tier=false + group_tier {群名:1~4}）；群员黑名单（group_blocklist：不存档/不触发/不进提示词）；表情包积极度 sticker_level 0~3
- 记忆隔离：默认每群独立记忆池（share_across_groups=false），群间互不串味；控制台一键切共享
- 反应评分引擎（scoring v1）：本地正反馈评分 + 内置趣味种子库（few-shot 参考，零 token 防饱和）；可选导入金句墙种子、可选用 LLM 在线打分（默认关）
- 种子库扩充：官方精选 **91 条**（data/seed_library.json，来源：知乎神回复精选、央视新闻/南国早报「晚安十点」神回复（含蔡澜/契诃夫/加缪/鲁迅/王尔德/胡适名句）、群聊接梗沙雕短句）；实测学习效果：热烈回应话题 +1.5 分封顶、冷场话题降权出榜、7 天热度半衰期精确
- 语言风格训练防「左右脑互搏」：角色设定提到提示词最前（权重最高）+「最高原则：不能变人」；语言风格参考素材改为强约束（只借灵感不换腔调，角色卡不可能说的话一律不说）；实测：毒舌角色+哲理种子 → 毒舌腔调说出机灵话（维护角色卡为前提）；用户自定义角色卡同样生效
- 表情包：新增 collect_emoji（收藏群表情→data/emojis/ 截图收藏，UIA 精确截图）/ list_emojis（查看收藏夹）/ send_emoji（从收藏夹发表情）；微信卡新增「表情包收藏夹」可视化管理（缩略图+刷新+计数）
- 查看合并转发：新增 view_merge_forward 工具——解析 zstd XML 里多条子消息标题/摘要，机器人可查看「合并转发的聊天记录」内容；read 时自动摘要 [合并转发] …，可调用工具看全文
- 控制台「调试·高级功能」导航：调整为「已实现（微信层）/高级功能（一般用户不用）说明」，不再罗列"做不到"清单（表情/合并转发已实现）
- 主动开话题（proactive，默认关）：群冷场超阈值后按概率主动抛话题，人设档位越高越活跃
- 社区分享（community，默认仅本地）：金句/意见/聊天记录一键导出 exports\；可选填上传 URL 直发社区（holyshits/feedback）
- 价目表：15 家厂商全模型单价（171 条内置表），每轮成本与 10 元可用轮数自动重算（scripts/gen_price_table.py 生成）

### 修复
- 保存设置真正生效：部分字段保存不再丢配置段（深合并基于全新拷贝），掩码 Key 不再覆盖真实 Key（余额/测试 API 不再报 latin-1 错误）
- 响应档位真实生效：1 档=仅艾特、2 档=+关键词、3 档=+随机（随机 60%）、4 档=全响应；关键词全角逗号识别、保存后不消失；控制台按档位联动显示（关键词 2/3 档显示、随机概率仅 3 档显示）
- 防"回应积压旧消息"：暂停/恢复不重放任务期间消息；历史上下文默认只带最近 30 分钟（可调）
- token 优化：省 token 开关（api.thinking，默认 off，省 50~90%）；工具轮数默认 8；运行明细显示推理 token 占比
- 发送输入：UIA SetValue 后台直写输入框（不抢焦点/不动鼠标）；发送后自动回后台并恢复原窗口（AttachThreadInput 提权 + 三级兜底，分条整批发完才恢复）；崩溃日志落盘便于排查
- 运行明细改简明日志（默认只显示群/状态/token/耗时/回复，详情默认隐藏、限高滚动）
- 引用失效：气泡文本定位、翻页滚轮修正、UIA 直输输入框；拍一拍误报修复（气泡菜单/OCR 过滤/行居中）
- 群选择统一搜索+滚动：首次向导选群、白名单弹窗、记忆页群下拉均支持搜索群名、列表滚动（群多不溢出）
- 可拖拽小鲸鱼：按住顶栏徽章鲸鱼拖出（跟手+速度拉伸+**被抓扭动挣扎**，幅度随时间衰减），松开**留在落点**后随机三态返回——①蠕动（波形+摆尾+缩身钻入，最慢）②平滑变形白纸飞机（morph 渐变）→ 抛物线滑翔飞回 → 入框翻回（较快）③扎入 UI 消失 0.5s → 从徽章冒出（气泡粒子+回弹，距离自适应）；**时长按拖拽距离计算**（蠕动 260ms/100px、纸飞机 170ms/100px+0.58s、扎入 90ms/100px+0.78s，控制台「光标设置」有 0.3~3 速度系数可调），每次返回打印 `[whale-return]` 调试日志（距离/方式/时长）
- 三步引导升级：第 1 步改为**选厂商（15 家）→ 选模型（160+）→ 填 API Key** 三联动（换厂商自动切 Base URL/模型列表/Key 格式提示）；向导弹窗适配鲸落/浅色/深色全部主题（输入框/搜索框支持主题变量）
- 控制台主题：默认 🐋 鲸落（深海蓝渐变 + 光斑漂移），可切 浅色 / 深色 / 跟随系统，「界面适配」可选、保存设置生效；URL 加 ?theme=whale 可临时预览
- 鲸鱼指针光标（可开关，默认开）：鼠标指针变成 DSniang 小鲸鱼（点击时向下点头），控制台「界面适配」可选 ui.whale_cursor
- README 升级图文落地页：顶部 Logo + 9 宫格功能卡 + 快速安装/功能/配置/控制台/运维/风险提示/致谢导航
- 一键启动.vbs（替代 bat）：依赖检查 →（缺则自动安装）→ 自检 53 项 → 自动启动机器人，零弹窗后台（onestart.py 落盘日志 logs\onestart.log）；保留 启动机器人.vbs（仅启动）/ 停止机器人.vbs（停止）作备用
- 停用旧 bat 启动器（避免 cmd 黑框）：一键启动仅用 vbs + 控制台一站式（体检/暂停/恢复/停止/重启/测试 API/查余额/设置/光标/主题）
- 图标素材重做：「11 绿底 + 111 纯白鲸鱼主体（眼睛透绿底）」徽章；弹跳改为**原地果冻弹跳**（下蹲-拉伸起跳-落地压饼形变明显，无位移）；光标用「22 蓝鲸+白色微信气泡」保留原色仅抠背景
- 鲸落主题转正为**默认出厂视觉**（深海蓝渐变 + 缓慢光斑漂移），可切换 浅色/深色/跟随系统
- 控制台补全可调项：拍一拍（回拍概率/冷却/主动皮）、记忆共享、主动开话题、统计周期、按型号单价 JSON、导出目录；group_blocklist/model_prices 支持 JSON 文本安全落盘
- 回拍配置真实生效（此前 90%/30 分钟硬编码）；selftest 补 wx_agent/scoring 导入检查（防隐藏 NameError）
- 首次向导修复：加载配置后再弹（此前时序问题不显示）；完成/跳过只弹一次（不再"完成又跳回"）；老用户（Key 已配置）不打扰
- API Key 输入：模型 API 卡新增「保存 Key」按钮（立即保存+测试连通）；切换任何厂商（含默认 DeepSeek）且无 Key 时弹窗提醒填写
- 自我昵称修复：@识别兼容微信实际昵称（自设 persona.self_nickname 后，用微信真名 @ 也能触发），同时认 自我昵称/微信真名/人设名
- 概览群名列表加滚动槽；机器人停止后自动关浏览器标签（window.open+close 绕过拦截，兜底空白页）
- 依赖补齐：pypinyin（wechatauto 依赖，离线 wheel 已同步）；在线安装改走 requirements.txt（此前硬编码易漏包）
- 代码精简：删除 10 个未使用的死函数（util/llm/store/tools/wechat/safe_fetch）
- 其余：点击归属校验与自动置前重试、发送与模型互抢窗口串行锁、noreply 自动补发、成本估算与周期统计、bat/VBS 中文乱码（GBK+CRLF）、控制台导航/下拉补齐、微信版本与依赖校验

### 已知限制
- 收藏表情库/合并转发未实现（微信自绘 UI）
- 海外端点需可访问该厂商的网络（地区限制除外）
- 数据库/微信界面随官方更新可能需升级适配层（体检会提示）
