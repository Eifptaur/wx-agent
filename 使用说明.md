# 使用说明（源码包 wx-agent-vβ·Ⅱ.zip）

本文档面向第一次使用的零基础用户，只讲怎么装、怎么用。

## 一、准备

- 电脑：Windows 10/11，已安装微信 4.1.13（微信官网下载安装）。
- 模型 API：准备一个 OpenAI 兼容的 API Key（如 DeepSeek 官方 `platform.deepseek.com` 充值后创建）。
- 网络：本说明为「联网安装」；无网电脑请看 `offline\README-离线包.txt`（用法不同，别弄混）。

## 二、安装（两步）

1. 解压 `wx-agent-vβ·Ⅱ.zip` 到任意英文路径（如 `C:\wx-agent`）。
2. 双击 **`一键启动.vbs`**。

它会自动完成：安装/找到 Python → 安装依赖（已装则跳过）→ 环境自检（55 项）→ 启动机器人 → 打开控制台。
没有弹窗、不需要命令行，进度写在 `logs\onestart.log`（也会显示在控制台"运行日志"里）。

## 三、首次使用（按顺序，都在控制台里操作）

1. **填模型**：左侧菜单「模型 API」→ Base URL 填 `https://api.deepseek.com/v1` → API Key 粘贴你的 Key → 点「测试 API 连通」出现 ✅ 后点「保存设置」。
2. **选监听群**：「微信」→「检测群聊并勾选」→ 勾选你要机器人聊天的群 →「保存设置」。
3. **选人设**：「人设与响应」→ 选一个人物（如 小鲸鱼）→「应用」。
4. **开始运行**：顶栏点「恢复」；再点「暂停」可随时停。

## 四、日常操作

| 想做什么 | 怎么做 |
|---|---|
| 启/停机器人 | 「一键启动.vbs」启动；「停止机器人.vbs」停止 |
| 开机自启 | 管理员运行 `scripts\开机自启.bat`；取消用 `scripts\取消开机自启.bat` |
| 换模型/厂商 | 「模型 API」页切换厂商（内置 DeepSeek/智谱/Kimi/ChatGPT/Claude 等），改动后点「保存设置」 |
| 换人设 | 「人设与响应」→ 选人设 → 应用；可先「模型评分」看这卡贴不贴，或「模型补足」自动改好 |
| 看花销 | 「概览」页：今日/周期/累计成本与 token；每日明细日历点日期查看 |
| 删计费日志 | 「概览」右上角「☑ 勾选删」（按天勾选、可定位日期、确认后真删） |
| 体检 | 「检测中心」：代码检测（快，不动鼠标）/ 鼠标操作检测（会动真实鼠标，约 40~70 秒，期间别碰鼠标） |
| 出问题 | 「运行日志」看报错；代码检测失败看「代码检测」面板 |

## 五、配置项保存规则

- 改动任何设置后点对应页的「保存设置」或右上角「保存全部设置」，保存后自动刷新生效。
- `config.json` 是配置文件（含 API Key），**不要发别人、不要上传**；重置 Key 用「重置 Key」按钮。

## 六、常见问题

- **双击 vbs 没反应**：看看 `logs\onestart.log`；多半是依赖未装或网络问题，重试即可。
- **控制台打不开**：双击「一键启动.vbs」会自动打开；手动访问 `http://127.0.0.1:3210/`，口令在日志里。
- **机器人不回话**：确认已「恢复」、已勾选目标群、群里 @ 了机器人或命中了设定关键词。
- **微信版本更新后失灵**：到根目录运行「检查微信版本.bat」，按提示升级适配层后重启机器人。
