wx-agent 使用说明（超详细版）

本文档手把手带你从零把机器人跑起来。每一步都写清楚了「在哪个窗口、点哪里、输什么」。


⚠️ 风险提示（基于公开案例与社区实测数据查证，2026-09）

微信官方协议禁止非官方客户端 / 自动化程序，使用有真实案例。公开数据（腾讯云开发者社区 2025-04，微信机器人框架作者对 200+ 用户的调查）显示：
· 封号重灾区是「Hook / 进程注入」类（如 WeChatFerry、WCF、多开外挂），接近"用了必中"；官方明确打击清单还有：自动加好友、批量点赞/评论/转发朋友圈、抢红包插件。
· 同一调查中约 15% 的机器人用户从未被封过；微信版本、运行环境都不是决定性因素——决定性的更多是"行为画像"（频率、模式、举报）。
· 被处理的后果分档：限制功能（临时停止加人/要求验证）→ 短期封禁（多数可申诉/到期解）→ 永久封禁（不可逆，最常见于屡犯 + 营销行为）。

本项目的定位：UIA 无注入——不 Hook、不改微信文件、不碰协议，纯"读本地数据库 + 模拟真人键鼠"，**不在上述打击清单内**；但它仍是"非官方行为"，不确定性和检测风险无法归零（回复过快、话术模式化、被多人举报仍可能触发风控）。

建议（照做能把风险压到很低）：
1. 用有使用时长的小号，主力微信号绝不要启用；
2. 控制频率（默认每分钟 ≤20 条，不建议调高）；不群发、不深夜高频、不做任何"刷数据"性质的事；
3. 机器人发声期间别抢它的键鼠（操作打架是异常行为的经典特征）；
4. 定期看控制台"版本体检 + 微信守护"状态，异常及时停用。

结论：风险"不是必封、也绝非零"——同类方案里属于温和派，但用真实微信号本身就有不确定性。是否使用、用哪个号，请自行评估并承担后果。

快速安装（三步速览）

1. 装环境：Windows 10/11（64 位）+ Python 3.10+，双击 安装依赖.bat（已装好且版本正确自动跳过，缺什么装什么）。
2. 跑起来：登录电脑微信 4.x（小号，勾选自动登录）→ 双击根目录「启动机器人.vbs」（无窗口）→ 浏览器自动打开控制台 → 首次向导：填 Key → 勾选群 → 体检 → 完成。
3. 测试：群里 @机器人 说句话即可（全程不用改任何文件，之后都在控制台改）。

详细分步（含离线安装、Web 控制台、识图/拍一拍/发图、常见问题）见下方第 3~10 步。


致谢：本作品整合自以下开源项目（经二次开发整合，仅供学习交流；上游版权归原作者，许可证随包附送）：
- QQ Agent（智能大脑）：由 B 站网友 Kondius 基于 qq-bridge（https://github.com/Derpyu520/qq-bridge）修改而来。相关 B 站视频 BV1ss8R6zERG；修改版下载地址 https://t.bilibili.com/1244553403559837713
- wechat-deepseek-bot（微信接入层，底层 wechatauto UIA 无注入）：B 站视频 BV1Mz4267EHQ，GitHub https://github.com/bdydgz114514/wechat-deepseek-bot
- DeepSeek-Balance-Whale-Widget（小鲸鱼余额挂件）
接入方式为 UIA 无注入：不注入、不 Hook、不改微信文件。

不过尽管有这几个项目珠玉在前，但是做兼容和编写新的功能仍然十分困难，最终消耗的精力几乎相当于重构。

## 1. 这个项目是什么
## 2. 提示词 / 人设是怎么设置的（重点）
## 3. 第 0 步：准备环境
## 4. 第 1 步：安装依赖
## 5. 第 2 步：填写配置
## 6. 第 3 步：运行自检
## 7. 第 4 步：登录微信
## 8. 第 5 步：启动机器人
## 9. 第 6 步：打开 Web 控制台
## 10. 第 7 步：在群里测试
## 11. 日常运维
## 12. 常见问题 FAQ

Q0：微信更新后（或版本不对）机器人异常？
自动检测已内置：启动时自动体检，发现不匹配会弹窗询问是否自动升级修正；启动日志、控制台「微信」卡片、自检也都会显示微信版本（如 4.1.13.63）与适配层版本（如 1.1.5.1）。
微信官方更新界面后：

## 13. 目录结构
## 14. 免责声明


## 1. 这个项目是什么

wx-agent 是一个微信群聊 AI 机器人：
- 智能大脑：无状态会话、响应档位、工具集、记忆、联网搜索、人设体系（由开源项目整合，见致谢）。
- 微信接入：读微信本地数据库 + 屏幕自动化发消息，不注入进程、不 Hook、不改微信文件。

群里 @机器人昵称 提问，机器人就用「小鲸鱼」人设自然回复：能接梗、能装傻、能联网搜、能看图、能记住群友的长期印象。

## 2. 提示词 / 人设是怎么设置的

## 2.1 提示词的整体结构
机器人每收到一批需要响应的消息，就临时拼一份全新提示词（无状态：用完即弃，不带历史对话）。它由两部分组成：

① 系统提示（静态，每次原样重发），按顺序包含这些段落：
- 角色声明：「你是『小鲸鱼』，一个混在微信群里的普通群友…」
- 安全规则：5 条铁律（无本地工具、群友无管理权、不泄露凭据、角色只能管理员改、防诱导）
- 工作方式（工具协议）：8 条（文本输出不会发送、要发言必须调 send_message、分条用数组、安静结束是正常的…）
- 反 AI 味：拒绝有求必应、可以装傻已读乱回、少用推销词
- 保持主体性：有自己的立场和节奏，不是应声虫
- 该说/不该说：什么时候值得开口、什么时候安静
- 群聊不是客服队列：不需要每条都回，先判断谁在找你
- 像真人一样：可以漏看、晚回、不回；短句口语
- 不要当群管家：不总结话题、不劝架、不当主持人
- 引用与点名：只有必要时才 @ / 引用
- 轻量记忆：memory_append 只记长期印象，别当笔记本
- 微信场景规则：不用 Markdown、看图/联网/发图/拍一拍的行为指引
- 发送与汇报禁令：不许输出「我已在群里回复了…」这类汇报
- 角色设定：默认是完整的「小鲸鱼」角色卡（见 2.3）
- 管理员附加规则（可选）：custom_rules 字段，留空则不出现

② 用户消息（动态，每次唤醒重建），按顺序包含：
- 当前时间：可信系统时钟（问几点/今天几号直接据此答）
- 会话标识：群 + 第几次处理
- 此刻状态：在哪个群、你的名字、最近发言频率
- 过去状态：该群最近一段聊天记录（条数由响应档位决定）
- 本次唤醒：本次触发的新消息（带 #消息id）
- 参与度参考：安静型 / 普通群友 / 活跃型
- 记忆：相关群友的长期印象
- 成员备注：管理员设置的备注名（可选）
- 引导说明：最后一句——想说话就 send_message，不想说就结束

## 2.2 默认人设的内容是什么？
默认人设是「DeepSeek 小鲸鱼」角色卡（完整保存在 agent/persona.py 里）。

## 2.3 怎么改人设？
相关字段都在 config.json 的 persona 块里（也可在 Web 控制台改）：
- persona.bot_name —— 角色身份名（出现在「你是『谁』」），默认：小鲸鱼
- persona.self_nickname —— 群内展示名（留空 = 用 wechat.bot_nickname），默认：空。识别 @ 时同时认 自我昵称 / 微信实际昵称 / 人设名，填了与微信昵称不同也不会漏触发。
- persona.role_text —— 自定义人设文本。留空 = 用内置小鲸鱼角色卡；填了 = 完全替换，默认：空
- persona.participation —— 参与度 low / medium / high，默认：medium
- persona.custom_rules —— 追加附加规则（加在提示词末尾，不覆盖默认），默认：空

换人设示例：想让机器人变成「傲娇助手」，在 config.json 里把 persona.role_text 填上你的角色卡文本即可（可参考 agent/persona.py 里 aodiao / dushe 两个内置模板的写法）。想微调默认小鲸鱼又不想全删，就只改 custom_rules（例如填「回复永远不超过 5 个字」）。

## 3. 第 0 步：准备环境

## 3.1 系统要求
- Windows 10（20H2 及以上，含 LTSC 2021）或 Windows 11，仅 64 位
- 内存 4GB 以上（8GB 更稳）
- 能访问大模型 API（默认 api.deepseek.com，换厂商改 api.base_url）

## 3.2 安装 Python（必须 3.10 或更高）
1. 打开浏览器，访问 Python 官网 https://www.python.org/downloads/windows/
2. 点 Download Python 3.x.x（选 3.10~3.14 都行，64-bit）
3. 运行安装包，关键一步：在第一个界面勾选底部的 Add python.exe to PATH（不勾后面会很麻烦）
## 4. 点 Install Now
5. 验证：按 Win + R，输入 cmd 回车，在黑色窗口里输入 python --version，看到 Python 3.x.x 就成功。
   若提示「不是内部或外部命令」，说明 PATH 没勾上，重装勾上即可；或用 py -3 --version（Windows 自带 py 启动器）也能找到 Python，本项目脚本两种都兼容。

## 3.3 准备微信
## 1. 准备一个微信小号（建议有使用时长的小号，降低风控风险）
## 2. 电脑上安装微信 4.x（Weixin，不是老的 3.x）
## 3. 先别急着扫码，后面第 7 步再登录

## 4. 第 1 步：安装依赖

## 4.1 联网安装（推荐）
## 1. 进入 wx-agent 文件夹
## 2. 双击 安装依赖.bat
## 3. 脚本会自动装好全部依赖，看到「安装结束」即可。

（或者手动，在 cmd 里进入项目目录后执行：）
pip install wechatauto-replica==1.1.5.1
pip install requests urllib3 Pillow cryptography zstandard uiautomation comtypes pywin32 pyperclip psutil colorama winsdk imageio-ffmpeg

## 4.2 离线安装（目标电脑没网时）
项目内置了离线安装包（offline\ 目录），里面有三样东西：
- offline\wheels\ —— 全部依赖 .whl 包（Python 3.10 / 64 位版）
- offline\python\python-3.10.11-embed-amd64.zip —— 绿色版 Python 3.10.11 运行时
- offline\wechat\WeChatWin_4.1.13.exe —— 微信 4.1.13 安装包（版权归腾讯，仅离线安装用；已装微信请跳过）

方式一：目标电脑已装 Python 3.10（推荐）
1. 把整个 wx-agent 文件夹拷到目标电脑（U 盘 / 网盘）
2. 确认目标电脑 Python 是 3.10（64 位）：python --version 应显示 3.10.x
3. 双击 安装依赖.bat（先体检：已装好且版本正确自动跳过；检测到 offline\wheels 自动走离线模式），或手动：
   pip install --no-index --find-links=offline/wheels -r requirements.txt
4. 微信没安装 → 双击 offline\wechat\WeChatWin_4.1.13.exe 安装微信 4.1.13；已装跳过
5. 双击 自检.bat（53 项）确认全部通过后，双击 启动机器人.vbs 开跑

方式二：目标电脑连 Python 都没有（完全离线）
1. 解压 offline\python\python-3.10.11-embed-amd64.zip（绿色版，免安装）
## 2. 用这个 Python 装依赖（它本身不含 pip，需先装 pip）
## 3. 之后用它运行机器人

关于 Python 版本的重要说明：
- offline\wheels 里的二进制包是 cp310 版（为 Python 3.10 打包），只保证在 Python 3.10 上离线安装成功；在 3.11/3.12/3.14 上这些 cp310 的包装不上（纯 py3-none-any 的包不受影响）。
- 若目标机器用其它 Python 版本，请在有网机器上重新下载匹配的 wheel：pip download -r requirements.txt -d offline\wheels，再拷过去。

依赖清单在 requirements.txt（共 15 个包），其中 wechatauto-replica 为微信接入核心库（PyPI 上可直接安装）；在线安装走 requirements.txt 全量安装（不再硬编码包列表），离线依赖全部封装在 offline\wheels（含 pypinyin 等间接依赖）。

## 5. 第 2 步：填写配置（推荐只走控制台，无需改文件）

首次启动（双击 一键启动.vbs）后，浏览器自动打开控制台并弹出**三步向导**：
1. **选厂商（15 家）→ 选模型（160+）→ 填该厂商 API Key**（切厂商自动换 Base URL/模型列表，并提示 Key 格式；DeepSeek/Kimi/智谱/通义/MiniMax/豆包/ChatGPT/Claude/Gemini/Grok 任一厂商均可；按厂商存 Key）；
2. 检测微信群聊 → 勾选要回复的群（= 白名单）；
3. 一键体检（53 项）→ 完成。

以后改任何设置都在控制台对应分区改，**保存即生效**，不需要碰 config.json。
config.json 由控制台自动生成/维护；下列字段说明仅为底层速查（高级用）。

## 5.1 想手动编辑时（不推荐，仅高级用户）
1. 进入 wx-agent 文件夹
2. 右键 config.json → 打开方式 → 记事本（或任意文本编辑器）
3. 改下面几处；改完重启机器人生效

## 5.2 快速上手字段
"api": {
  "base_url": "https://api.deepseek.com/v1",
  "api_key": "sk-你的真实密钥",
  "model": "deepseek-v4-flash-vision-exp"
}
- 不限于 DeepSeek：控制台「模型 API」内置 15 家厂商 160+ 型号预设（DeepSeek / Kimi（K3/K2.7）/ 智谱 GLM-5.x / 通义 Qwen3.8 / MiniMax M3 / 小米 MiMo / 腾讯混元 / 百度文心 / 豆包 / ChatGPT·OpenAI（GPT-5.6 系）/ Claude（Opus5/Sonnet5）/ Gemini（3.8/3.7）/ Grok（4.6）/ NVIDIA / OpenRouter 聚合）+ 自定义 BaseURL，每家数十个主流型号；切换厂商自动回填该厂商已保存的 Key（api.provider_keys 按厂商存储）。
- api_key：对应厂商开放平台申请，DeepSeek 是 sk- 开头，在 platform.deepseek.com 获取。
- model：默认视觉模型 deepseek-v4-flash-vision-exp（能看图 + 联网）；想纯文本省钱就换纯文本模型并把 api.vision 改成 false。

"wechat": {
  "bot_nickname": "群deepseek"
}
- bot_nickname：改成机器人微信小号在群里的真实昵称（比如小号叫「阿蓝」就填 阿蓝）。群里 @阿蓝 你好 才会触发机器人。

## 5.3 常用可选配置（按需改）
- api.temperature —— 活泼度 0~1，越高越天马行空，默认 0.8
- api.thinking —— 思考模式 auto/on/off，**默认 off**；模型返回的「推理文本」是生成的思考链式输出（非真实内部思维），按输出价计费且通常占 token 大头——off 可省 50~90%
- api.max_rounds —— 单次最多调几轮工具，默认 8（每轮都重发上下文，调低更省 token）
- api.vision —— 是否启用看图，默认 true
- api.price_input_per_m / price_output_per_m / price_cached_per_m —— 自定义单价（元/百万 token）；留 0 用内置价（内置 171 条官方/参考单价表，覆盖 DeepSeek/GLM/Kimi/MiniMax/MiMo/通义/混元/豆包/文心/OpenAI/Claude/Gemini/Grok/OpenRouter 等全部预设型号；未知型号按主流档位估算，可用 api.model_prices 按模型精确覆盖）
- api.use_official_price —— 是否用内置官方单价表估算成本，默认 true
- wechat.group_name_white_list —— 只在这些群里回复；[] = 所有群
- wechat.poll_interval —— 消息轮询间隔（秒），默认 3
- wechat.start_paused —— 启动后默认暂停，默认 true
- persona.bot_name —— 人设名，默认 小鲸鱼
- persona.participation —— 参与度 low/medium/high，默认 medium
- store.context_tier —— 响应档位 1~4，默认 2（关键词档：省 token 又够活跃）
- store.unified_tier / group_tier —— true=全局档位统一（默认）；false=可按群单独设档（{群名: 1~4}，未设置的群跟随全局）
- store.group_blocklist —— 群员黑名单（{群名: [昵称, wxid...]}）：被屏蔽者不存档、不触发机器人、不进提示词
- store.sticker_level —— 表情包积极度 0~3（不鼓励/偶尔/较积极/爱好者），控制台「社区与学习」卡可调
- memory.share_across_groups —— false=每群独立记忆（默认，群间不串味）；true=所有群共享一个记忆池
- proactive.enabled / check_interval_min_ms / idle_threshold_ms / probability —— 主动开话题（默认关）：群冷场超过阈值后按概率抛话题
- scoring.enabled / seed_library / online_scoring / import_seed_file —— 反应评分引擎（本地正反馈 + 趣味种子库；online 打分费 token 默认关；可从金句墙导出的 JSON/文本导入种子）
- community.export_dir / holyshits_upload_url / feedback_upload_url / upload_enabled —— 社区分享：金句/意见/聊天记录本地导出到 exports\；填 URL 可直发（默认只本地）
- poke.reply_probability / cooldown_seconds / active_probability / active_daily_limit —— 拍一拍：回拍概率/冷却/主动皮概率/每日上限（控制台「拍一拍」卡可调）
- stats.period —— 用量统计周期 daily/weekly/monthly（控制台「服务器」卡可调）
- api.model_prices —— 按型号单价 JSON（控制台「模型 API」卡可调，优先于内置价）
- store.keywords —— 2 档关键词，默认 []
- store.past_window_min —— 历史上下文时间窗（分钟），默认 30；0=不限
- web_search.provider —— 搜索引擎，默认 bing
- send.max_per_minute —— 每分钟最多发几条，默认 20
- server.port —— Web 控制台端口，默认 3210
- server.token —— 控制台访问口令；留空 = 启动自动生成随机口令并存回 config.json
- server.auto_open_browser —— 启动后是否自动开浏览器，默认 true
- ui.theme —— 控制台主题 whale/light/dark/system（默认鲸落深海蓝；可切浅色/深色/跟随系统）
- ui.whale_cursor —— 鲸鱼指针光标开关，默认 true（点击时小鲸鱼向下果冻点头）
- ui.cursor_image —— 自定义光标（「光标设置」页上传后用 custom-cursor.png；留空=默认鲸鱼 22）
- ui.whale_anim.worm / plane / zap —— 拖拽返回动画速度系数（0.3~3，默认 1；蠕动最慢、纸飞机较快、扎入距离自适应；时长 = 距离×系数，公式见控制台「光标设置」页）

> 浏览器兼容：控制台支持 Microsoft Edge 80+（Win10/11 自带，推荐）、Google Chrome 80+（含 QQ/360 极速/搜狗等 Chromium 内核）、Firefox 74+、Safari 13.1+；**不支持 IE 11**（页面会白屏）。

## 5.4 响应档位详解（省钱关键）
store.context_tier 决定机器人「什么时候回」：
- 档位 1：只有被 @ 才回，其余消息标记已读、不调模型（最省）
- 档位 2：被 @ 或命中 store.keywords 关键词才回
- 档位 3：以上 + 普通消息按概率随机回
- 档位 4：任何消息都回（最费）
没命中时不产生任何 token 消耗，消息仍会记进存档，日后被 @ 时作为「已读历史」带上。

> 档位在控制台保存即真实生效（1 档不会"像 4 档每条都回"）；关键词支持全角逗号「，」分隔。
> 历史上下文默认只带最近 30 分钟内的消息（store.past_window_min，0=不限），所以机器人不会翻很久之前的旧艾特/旧话题来回答；若群很少说话想要更多历史，把该值调大（如 120）。

## 5.5 启动后默认暂停（防开机刷群）
机器人默认**启动后即暂停**（wechat.start_paused=true）：不监听任何群消息，控制台顶栏显示「已暂停」，点「恢复」才开始工作。暂停期间群里的消息会被跳过（恢复时不会重放），适合"想用的时候才让它上线"。取消勾选（控制台「微信」卡 → 启动后暂停）可改为开机自动运行。

## 5.6 发送窗口行为（不打扰你）
机器人发送消息时：文本用 UIA 后台直写（不需要微信在前台）；仅「搜索群聊/回车发送」需要瞬间把微信置前（微信自绘界面硬限制），**发送完成立即把微信放回后台、恢复你正在用的窗口**（多次实测：你用记事本/浏览器时，机器人发完消息会精确回到你正在用的窗口）。若你正看着微信窗口，机器人不会动它。分条回复会一次性连续发完才恢复，不会闪来闪去。

## 6. 第 3 步：运行自检

## 1. 双击 自检.bat
2. 看输出：应显示「共 53 项」；唯一红的是 api.api_key（如果你还没填真密钥）
## 3. 填好真密钥后再跑一次，应看到全部通过（51/51）

自检会检查：Python 版本、全部依赖包、全部项目模块（含小鲸鱼挂件 agent.whale）、纯逻辑、配置（API/模型/视觉）。

## 7. 第 4 步：登录微信

## 1. 打开电脑桌面微信 4.x
## 2. 用手机微信扫码登录小号
## 3. 手机上勾选「自动登录该设备」（重启后微信自动登录，不用再扫码）
4. 让微信窗口保持打开（可以缩小，但别最小化到任务栏——机器人发消息靠模拟键盘鼠标，窗口最小化就发不出去）

## 8. 第 5 步：启动机器人

## 1. 双击根目录「**一键启动.vbs**」（推荐：零弹窗后台自动 依赖检查 → 缺则自动安装 → 自检 53 项 → 启动机器人；进度看控制台「运行日志」或 logs\onestart.log）
2. 也可以双击「启动机器人.vbs」（仅启动，零窗口备用开关）；全程**不弹任何命令行窗口**：vbs → 看门狗、机器人都是 pythonw 隐藏运行，不依赖 PowerShell / cmd / 黑框
3. 如何确认在跑：任务管理器里能看到 pythonw.exe（看门狗 + wx_agent.py 各一个），或打开 Web 控制台看到「开始监听」；进程异常退出时看门狗 5 秒后自动拉起
4. 停止：双击根目录「停止机器人.vbs」——弹窗会详细说明停止效果（机器人+看门狗都停、不再自动拉起）、恢复方式（启动机器人.vbs / 一键启动.vbs）、群聊数据不丢失、日志位置
5. 网页端也可停止/重启：控制台顶栏「停止」「重启」（点击后弹出详细确认框，图标为果冻弹跳鲸鱼徽章）

> 所有日常操作（体检/暂停/恢复/停止/重启/测试 API/查余额/修改设置/光标/主题）在 Web 控制台一站式完成，浏览器默认 Microsoft Edge 80+ / Chrome 80+（不支持 IE 11）。

注意：机器人用 UIA 读本地数据库、发消息时模拟在微信窗口打字回车，期间你会看到微信窗口自己动，这是正常现象。发消息时别抢鼠标键盘、别最小化微信。

## 9. 第 6 步：打开 Web 控制台

启动后会自动用默认浏览器打开控制台（网址带随机口令，形如 http://127.0.0.1:3210/?token=xxxx）。若没自动打开，看启动日志里「Web 控制台」那行网址，复制到浏览器打开。

控制台几个板块：
- 状态：机器人是否在线、目标群列表、累计会话数/总 token/已发消息数/累计成本 + 「今日/本周/本月」周期用量卡（用量按周期重置，历史保留 24 期；成本按模型价目表估算，未知型号按主流档位兜底，可用 api.model_prices 精确覆盖）
- 运行明细：每轮对话的思考过程（可展开）、token 用量、成本、工具调用、回复摘要（服务端按天落盘）
- 余额（顶栏）：显示 DeepSeek 账户余额（充值 + 赠送），点击刷新，每 30 秒自动刷新
- 模型 API：改 Base URL / Key / 模型 / 温度，点「测试 API 连通」验证
- 微信：改机器人昵称、轮询间隔、群白名单
- 人设与档位：改人设名、参与度、响应档位、关键词；**按群单独设档**（关掉「所有群统一档位」后给每个群设 1~4 档）、群员黑名单、表情包积极度
- 记忆：逐群查看/搜索群成员长期印象，每行可「编辑」（新增/修正印象）；「共享记忆池」开关（默认每群独立）
- 社区与学习：反应评分（趣味种子库、热度衰减）、导入金句墙种子、金句/意见/聊天记录本地导出（exports\）、可选填写上传 URL 直发社区
- 联网搜索：开关 + 换引擎
- 完整 JSON：直接改整个 config.json（高级）
- 运行日志：实时滚动看最近 500 条日志
- 暂停 / 恢复：一键暂停或恢复机器人
- 停止 / 重启：**停止 = 连看门狗一起停**（机器人+看门狗都结束，不会再 5 秒后自动拉起来、也不会多弹新控制台）；重启 = 先停旧看门狗，再由新看门狗无窗口接管新实例
- 界面适配（DPI / 遮挡 / 主题）：显示缩放（自动/100/125/150/175/200%）、控制台主题（默认🐋鲸落/浅色/深色/跟随系统）、点击前清遮挡开关、「鼠标点击自检」按钮
- 光标设置：鲸鱼光标开关（默认开，鼠标变小白鲸、点击时向下果冻点头）；可上传自己的 PNG/JPEG 图片作为光标（上传→预览→保存生效→可重置回默认鲸鱼）；顶栏徽章与所有弹窗图标为「绿底背景 + 蓝鲸线稿」果冻弹跳
- 小鲸鱼余额挂件：右下角常驻（见 9.1）

改完点「保存设置」。注意：暂停/恢复、测试 API 即时生效；换模型/人设/白名单等要重启机器人才完全生效。

## 9.0 界面适配与跨电脑兼容（不同 DPI / 多显示器 / 触屏）

机器人的屏幕操作（点击类）统一走 agent/ui_adapt.py 适配层，解决换电脑时的三类问题：

- 显示缩放不同：自动检测（物理分辨率 ÷ 进程感知分辨率，对 100/125/150/200% 都正确），点击坐标自动换算；也可在控制台「显示缩放」里手动指定。
- 系统叠加层：Windows 触控键盘/手写输入画布（TabTip / ShellHandwritingCanvas）全屏置顶时会截走点击（表现为「微信能看见但点了没反应/菜单不弹出」）——点击前自动关闭它（先 WM_CLOSE + 隐藏，TabTip 兜底强制结束）。
- 遮挡窗口：点击前校验「这个点当前是不是微信接收」，被浏览器等窗口挡住就自动最小化，清理不掉就如实反馈（报告里写明是谁挡的）。

「鼠标点击自检」按钮的作用：把鼠标移到微信窗口中央并回读——显示检测到的缩放系数、光标最终位置、命中的窗口（类名/标题/进程），判断是不是微信。换一台电脑后先在控制台点一次自检，能立刻判断是缩放问题、遮挡问题还是命中测试问题。

重要：机器人请在**真实桌面会话**里运行（双击「启动机器人.vbs」或自带终端），不要从「托管/远程编程助手」跑的命令行启动——那些环境的输入虚拟化会把所有窗口的命中测试变成桌面，点击类功能（拍一拍/引用/@）会全部失效但发消息正常。自检若显示「所有窗口都命中桌面」，即属于这种情况，换正常会话即可。

9.0.1 跨电脑适配清单（哪些功能依赖屏幕、怎么适配）

| 功能 | 是否碰屏幕 | 适配方式 |
| 读消息 / 下载图片 / 存档 | 否（纯微信数据库） | 与电脑无关 |
| 发文字 / 发图 | 键盘+回车为主（UIA），无坐标问题 | 回退到「点发送按钮」时也走适配层 |
| 打开会话 open_chat | UIA 为主；搜索回退会点击 | 点击走适配层 |
| 拍一拍 send_poke | 右键头像 + 菜单 | 适配层（DPI 换算 + 遮挡清理 + 归属校验）+ UIA 行定位 + 数据库验证 |
| 引用 reply_quote | 右键气泡文本 + 菜单 | 头像定位（与拍一拍同链路）+ 气泡起点命中测试 + UIA 直输输入框 |
| @ 成员 at_member | 输入 @ + 成员弹层点击 | 适配层 |
| OCR（消息区/菜单/输入框探测） | 截图（与 GetWindowRect 同空间） | 缩放不一致时用 ui.coord_scale 覆盖 |
| 界面适配自检 | 鼠标移动+回读 | 控制台「鼠标点击自检」按钮 |

拍一拍行为规则（config.json 的 poke 段可调）：
- 别人拍一拍机器人 → 系统自动回拍（默认 90% 概率，同一人 30 分钟冷却；先自然回应一句，延迟约 18 秒再拍）
- 「皮一下」主动拍人 → 低频：默认 10% 概率 + 每天最多 3 次，被拦时如实说没拍
- 群友明确说「拍一下某人」→ 直拍不受门控；send_poke 的 reason 参数（reply/request/playful）由模型按规则选
- 一键体检「适配·点击实测」与「拍一拍检测」走同一条链路（定位→右键→菜单识别→Esc），拍一拍检测可用时体检该项即通过

## 9.1 小鲸鱼余额挂件

右下角常驻小鲸鱼娘（网页右下角，别和微信窗口弄混）：
- 余额：60 秒自动刷新（点鲸鱼立即刷新），数字滚动动画；「今日已用 ¥xx」按每次调用真实 token 记账（账单在 data/whale-state.json，跨天归零、保留 30 天）
- 每轮对话消耗：机器人每完成一轮回复，气泡弹出「上一轮对话消耗: ¥xx」（可关、可改自动关闭秒数）
- 拖拽吸附：四边/角落自动吸附；贴左边时镜像翻转；汉堡菜单可调大小/音效/气泡开关，设置自动保存
- 随机台词：点气泡换一段（峰谷提示/今日已用/卖萌吐槽，偶尔 gif），不点 5 秒自动收起

注意：挂件走同一个控制台访问口令（网址 ?token= 已自动带上）。

## 10. 第 7 步：在群里测试

10.1 文字问答：在目标群里发 @你的机器人昵称 你好，机器人会以「小鲸鱼」口吻回复。
10.2 识图：群里发一张图 → 右键引用这张图 + 发 @机器人 分析这张 → 机器人下载解密看图后描述内容。
10.3 联网：发 @机器人 今天的哈尔滨天气怎么样 或 @机器人 讲个笑话，机器人会自动联网搜索再答（讲笑话会去 B 站/贴吧搜神评）。
10.4 发图/拍一拍/引用：让机器人发图、拍一拍某人、引用回复，看各项工具是否正常。

## 11. 日常运维

- 停止机器人：双击根目录「停止机器人.vbs」（按 PID 文件安静结束机器人和看门狗，无黑框）
- 重新启动：双击根目录「启动机器人.vbs」
- 备份配置+存档：双击 scripts\备份配置.bat（备份到 backups\）
- 开机自动启动：双击 scripts\开机自启.bat（用任务计划注册登录自启）
- 取消开机自启：双击 scripts\取消开机自启.bat
- 看运行日志：打开 logs\wx_agent.log，或 Web 控制台「运行日志」
- 换电脑：整个 wx-agent 文件夹拷走，按第 3~8 步走一遍

重启后的流程：开机自启 + 微信「自动登录该设备」都配好后，电脑重启 → 微信自动登录 → 机器人自动启动，全程无需人工干预。

## 12. 常见问题 FAQ

Q0：微信更新后（或版本不对）机器人异常？
自动检测已内置：启动时自动体检，发现不匹配会弹窗询问是否自动升级修正；启动日志、控制台「微信」卡片、自检也都会显示微信版本（如 4.1.13.63）与适配层版本（如 1.1.5.1）。
微信官方更新界面后：


Q1：@ 了机器人没反应？
依次检查：① 微信是否在线、是否最小化；② 是否 @ 对了昵称（= wechat.bot_nickname）；③ 群名是否在白名单（空=不限）；④ 看 logs\wx_agent.log 报错。

Q2：识图失败 / 提示图片解密密钥未就绪？
首次识图前，先在电脑微信里手动点开任意一张图片看大图（让密钥加载进内存），机器人会自动捕获；之后就能自动下载解密了。

Q3：会封号吗？
UIA 模式不注入、不 Hook，只模拟正常操作，风险远低于注入方案；但仍建议用小号、控制提问频率（默认每分钟上限 20 次）。

Q4：杀毒软件报毒？
UIA 模式不含注入组件，一般不会误报；如被拦截，把 wx-agent 目录加入信任区。

Q5：重启电脑后要重新扫码吗？
不需要——首次登录时手机勾选了「自动登录该设备」。

Q6：费用多少？
仅 DeepSeek API 按量计费。闲聊一整天通常几分钱；store.context_tier 设 1 档最省。

Q7：微信自动升级影响机器人吗？
UIA 模式适配微信 4.x 全系，界面布局会自动重新校准。

Q8：端口 3210 被占用？
机器人会自动顺延到 3211、3212…，看启动日志里「Web 控制台」那行的实际端口。

Q9：拍一拍失败 / 机器人说拍了但没拍上？
拍一拍是「右键聊天区里对方的头像 → 菜单选『拍一拍』」的屏幕自动化，分三层保障：①定位走 UIA 消息行（消息列表在 UIA 树里每行 Name 就是消息原文，目标行矩形精确匹配）+ OCR 相似度兜底；②点击后回读微信数据库，真的出现新的拍一拍事件才算成功；③失败如实反馈，不会再说「拍了」。
已知环境限制：微信右键菜单需要真实鼠标输入——在 150% DPI 缩放 / 多显示器镜像等环境下，合成的鼠标点击可能不生效（UIA 与键盘正常）。遇到「没拍上」时建议：①Windows 显示缩放设为 100%；②微信放主屏；③群里手动拍。建议微信用「浅色模式」（截图、OCR 与坐标体系用浅色版式校准过）。

Q10：小鲸鱼挂件不出现 / 只有个鲸鱼图没有气泡？
① 确认控制台网址带了 ?token=xxxx（没带口令会 401，挂件加载不出来）；② 浏览器强刷（Ctrl+F5）；③ 鲸鱼本体图是 whale-widget/assets/DSniang1.png，别删；④ 挂件菜单里「气泡」开关别关掉（气泡负责显示余额/今日已用）。

Q11：今日已用为 0 / 不对？
机器人启动后跑过几轮才会计账（账本从迁移那天开始记）。若机器人中途换过 Key 或多币种账户，账本按人民币记录，历史已过账金额不会重算。

## 13. 目录结构

wx-agent/
├── wx_agent.py / config.json / config.example.json / requirements.txt / LICENSE
├── README.md / 使用说明.md / 更新日志.md
├── 启动机器人.vbs / 停止机器人.vbs   ← 无窗口启动/停止（首选）
├── agent/               ← 智能大脑（config/llm/store/memory/sender/prompt/tools/wechat/webui/whale/ui_adapt 等）
├── whale-widget/        ← 小鲸鱼挂件原项目留档（MIT）
├── scripts/             ← 运维脚本（watchdog/stop_bot/自检/安装依赖/备份/开机自启）
├── offline/             ← 离线部署包（wheels + 绿色版 Python，可选）
└── data/  logs/  media/ ← 运行时生成（勿上传）

## 14. 免责声明

· 整合自多个开源项目（见致谢），仅供学习交流；上游版权归原作者，许可证随包附送；接入方式为无注入（不注入、不 Hook、不改微信文件），使用个人微信与账号风控请自行评估并承担风险。
- UIA 模式不注入、不 Hook、不改微信文件；使用个人微信与账号风控请自行评估并承担风险。
