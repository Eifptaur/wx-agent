# 检验清单（交付验收）

交付前言：本清单逐项对应"控制台 UI ↔ 功能"的一一映射与可操作验证，全部已在本机（Windows/微信 4.1.13 / Edge 自动化）实际跑通。

## 1. 配置项 ↔ 控制台映射（全量核对）

| 类别 | 结果 |
|---|---|
| Python 全量编译（agent/scripts/wx_agent） | ✅ 通过 |
| 前端 JS 语法（node --check 双脚本块） | ✅ 通过 |
| 静态资源引用（/assets/*） | ✅ 无缺失 |
| 元素 id 重复 | ✅ 无 |
| 配置键总数 188 → 控制台可调项 | 122 项直绑 data-cfg + 以下特殊绑定均已覆盖：厂商/模型下拉、参与度档、行为推荐卡（行为/表情档）、背景与光标上传、记忆共享群勾选、联网搜索分引擎、种子库按钮、拍一拍卡、省 token 开关、价格 JSON、发送限制滑条、检测按钮、语言风格下拉（正常/鲸语） |
| 未映射键 | 均为内部/运行时/预留键（`memory.use_chat_model` 等为预留未使用；`allow.*/deny.*`、`server.enabled`、`ui.poke_*` 等为内部状态或启动参数），无用户可调项遗漏 |
| 两套语言系统（正常 / 鲸语） | ✅ 均在「界面适配 → 界面文案风格」切换，保存即生效，双向可用 |

## 2. 控制台功能实测记录（全部通过）

| 功能 | 验证方式 | 结果 |
|---|---|---|
| 暂停/恢复 | 真实点击+API | ✅ 200，状态翻转，无报错 |
| 停止（确实能杀） | POST /api/shutdown → 4 秒 | ✅ 端口关闭、进程结束、stopped.flag 写入；重启正常 |
| 模型评分 | 含网梗卡 | ✅ 32.61~49.2 分（≤65 铁律）+ 缺陷点名 |
| 模型补足 | 污染卡 2 轮 | ✅ 去梗、最终 3 次复评中位 81.23，无虚高 |
| 人设库 | 185 张 | ✅ 评分 77.41~88.36 全唯一，评分表排序正常 |
| 计费勾选删除 | 弹窗全流程 | ✅ 真实账本按天聚合、勾选/一键今日本月、年月日定位（有/无记录两条路）、确认删真删+概览刷新 |
| 一键删计费 | API | ✅ 清文件+归零 |
| 代码检测/＋依赖核对 | 真实点击 | ✅ 0.5~2s 完成+toast+状态条+逐项面板+按钮恢复（此前后者卡死已修） |
| 厂商/模型下拉 | DOM 矩形+截图 | ✅ 关闭态无重叠；菜单不透明、z 层级正确 |
| 鲸语↔正常 | 双向真实保存 | ✅ 服务端注入+前端字典一致，反复切换正常 |
| 光标 | 默认蓝鲸 64px | ✅ 长按点头（nod 帧），自定义上传亦生成点头帧 |
| 记忆 | g1 假群清理 | ✅ /api/memory 无 g1；成员印象读写链路（隔离目录验证）正常 |
| 关机总结印象 | 停止触发 | ✅ 日志"关机总结印象中→完成"，写入链路正确 |
| 一键启动 | 系统/便携双路 | ✅ 系统 Python 识别；便携路径解压+离线 pip+21 依赖装成功 |
| 中转站兼容 | 本地 mock | ✅ 4 项：非 DS 无 thinking 参数 / DS thinking on/off / 500 可重试 |

## 3. 交付内容与归档

- ✅ 发布包：源码包 3.3MB（联网安装）+ 离线包 319.3MB（含绿色 Python/依赖/微信安装包），说明已按两包分开编写（README.md / 使用说明.md / offline\README-离线包.txt）
- ✅ 归档：开发交接/风险评估/开发守则、临时脚本 → `_scratch/archive_docs/`（不入库不入包）；历史完整日志 → `docs/changelog-archive.md`（历史可查，不入包）
- ✅ 未上传/未入库：config.json（含 Key）、data/、logs/、_scratch/、offline/wechat/、assets/wallpaper/*.mp4、custom-cursor*.png
- ✅ GitHub 说明（landing/release/about）留待上传后在平台侧撰写；仓库内 README/使用说明/更新日志已按"纯功能、无小巧思"重写

## 4. 备注

- 小巧思清单（不写入任何说明文档与更新日志，留待用户自行发现）：鲸鱼图标与光标设计、光标点击点头、鲸语文案风格、水光波纹、顶栏构建号、左下角彩蛋按钮（连点三下后再点一下出"档案"）。
- 新装机器建议跑一次「代码检测＋依赖核对」确认环境；微信大版本更新后运行根目录「检查微信版本.bat」。
