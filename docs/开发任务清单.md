# 开发任务清单（铁律：新任务必须登记；验证通过才核销；做完核查下一项）

> 规则（见 skill：wx-dev / wx-verify）：用户每次提出新任务 → 立即登记到本文件（状态=进行中）；
> 完成 = 通过 wx-verify 清单（编译/运行/联动/包验证四层证据）→ 才能标记 ✅ 并核销；
> 每完成一项工作，核查本清单，继续下一项；未验证的项不得标 ✅。

## 当前任务

- [⏳] **Token 优化**：模拟真实对话测每轮 token 基线（当前可能 >2000/轮）→ 搜索主流方案（上下文截断/记忆压缩/工具提示精简/系统提示瘦身/多轮摘要缓存）+ 自研点 → 优化 → 循环实测**稳定 <2000 token/轮**。目标：消耗稳定低于该值才算 ✅。
- [⏳] **dsh 插件补装评估**：dsh-agent-teams（npm 无）、@JasonFreeLab/dsh-superpowers（GitHub 独占）——网络恢复后经 dsh-community-plugins 评估再装；装前确认无冲突。
- [⏳] **重启 DSH 验证插件**：npm 全局插件（dsh-verify/skill-explorer 等 20+）重启后经技能中心核对全部加载、无冲突。

## 已完成（近期，均已验证）

- [✅] 双开控制台根治（端口感知探测 + 原子锁 + 单点打开 + Mutex 自愈；日志+截图验证通过）
- [✅] 微信窗口强制限位 1160×900（真机实测）
- [✅] 停止/重启生效（真机 shutdown→0 进程）
- [✅] 访问口令掩码 + 恢复端口按钮 + 端口保存提示（编译+联动检查）
- [✅] 安装依赖实时进度（@@PROG:install 逐包事件）
- [✅] 人设卡联网真实资料收集（注入补足与评分；接口实测 200/来源/语录）
- [✅] 群友深度印象（跨群采集+统计提炼；单元+接口实测）
- [✅] 机器学习仅默认卡启用（后端限制 + 说明）
- [✅] 图标全链路（exe 原图/窗口黑框/徽章透明白鲸；导出提取验证）
- [✅] 启动器全弹窗 UI 化（0 系统 MessageBox；csc 编译验证）
- [✅] 新机无 Python 无缓存启动测速 46.6s（写入更新日志）
- [✅] 启动器单点打开 + 弹窗联动确认；费用计算器（全厂商 171 条价目 + POST 修复 + 居中显示栏）

---
*规则更新方式：有新任务或新教训，立即更新本文件与 wx-dev / wx-verify skill（保持三处一致）。*
